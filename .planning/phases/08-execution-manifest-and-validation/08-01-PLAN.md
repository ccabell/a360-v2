---
phase: 08-execution-manifest-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/EXECUTION_MANIFEST.json
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03

must_haves:
  truths:
    - "Operator can look up any SQL file from phases 01-07 and see its execution status, phase, and dependency order"
    - "Operator knows which files must NOT be executed (out-of-sync / blocked)"
    - "Operator knows what to check before and after running each file"
  artifacts:
    - path: "supabase/EXECUTION_MANIFEST.json"
      provides: "Single dependency-ordered manifest of all SQL artifacts across phases 01-07"
      contains: "compile_sql"
  key_links:
    - from: "supabase/EXECUTION_MANIFEST.json"
      to: "supabase/compile_sql/*.sql"
      via: "file field in each manifest entry"
      pattern: "supabase/compile_sql/"
    - from: "supabase/EXECUTION_MANIFEST.json"
      to: "supabase/migrations/*.sql"
      via: "file field for migration entries"
      pattern: "supabase/migrations/"
---

<objective>
Build the single dependency-ordered execution manifest (EXECUTION_MANIFEST.json) listing all SQL files, TypeScript scripts, and migration files across phases 01-07.

Purpose: Create the single source of truth for what SQL exists, what order it runs in, what its execution status is, and what is blocked/out-of-sync -- so operators never run the wrong file or skip a dependency.

Output: supabase/EXECUTION_MANIFEST.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-execution-manifest-and-validation/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build EXECUTION_MANIFEST.json with all 52 compile_sql + 6 migration + 3 script entries</name>
  <files>supabase/EXECUTION_MANIFEST.json</files>

  <read_first>
    supabase/compile_sql/06-02-canonical-common-inserts.sql (confirm WARNING header for out_of_sync flag)
    supabase/compile_sql/05-01-execute-phase2-outstanding.sql (understand inline vs reference nature)
    compile_manifest.json (existing format reference)
    .planning/phases/08-execution-manifest-and-validation/08-RESEARCH.md (dependency graph, entry schema, status values)
  </read_first>

  <action>
Create supabase/EXECUTION_MANIFEST.json as a JSON object with the following top-level structure:

```json
{
  "manifest_version": "1.0",
  "target_project": "aejskvmpembryunnbgrk",
  "created": "2026-06-14",
  "total_files": N,
  "entries": [ ... ]
}
```

Each entry in the `entries` array follows this schema:

```json
{
  "file": "supabase/compile_sql/07-02-product-cadence.sql",
  "phase": "07-timing-rules",
  "plan": "07-02",
  "order": 42,
  "type": "schema|data|backfill|triage|meta",
  "execution_method": "supabase_dashboard_sql|tsx_script|migration_file",
  "depends_on": ["supabase/compile_sql/07-01-timing-schema.sql"],
  "status": "pending|applied|verified|deferred|blocked",
  "out_of_sync": false,
  "do_not_execute": false,
  "pre_exec_checklist": ["Human-readable prerequisite check"],
  "post_exec_verification": "SQL query or description to verify success",
  "source_artifact": "path to planning doc this was generated from (if known)",
  "notes": "Any special context"
}
```

**Dependency-ordered entries (use the `order` field, starting at 1):**

Layer 0 -- Pre-phase migrations (order 1-6):
1. `supabase/migrations/20260611_agent_manager_tables.sql` -- type: schema, method: migration_file, status: applied, depends_on: []
2. `supabase/migrations/20260612_add_does_not_solve_backfill_authority_rank.sql` -- type: schema, method: migration_file, status: applied, depends_on: []
3. `supabase/migrations/20260612_app_tracking.sql` -- type: schema, method: migration_file, status: applied, depends_on: []
4. `supabase/migrations/20260612_saved_outputs.sql` -- type: schema, method: migration_file, status: applied, depends_on: []
5. `supabase/migrations/20260612_search_reference_docs.sql` -- type: schema, method: migration_file, status: applied, depends_on: []
6. `supabase/migrations/20260613_timing_columns.sql` -- type: schema, method: migration_file, status: applied, depends_on: [], notes: "Mirror of compile_sql/07-01-timing-schema.sql"

Layer 1 -- Phase 1 citations (order 7-9):
7. `scripts/migrations/add_page_number_to_evidence_links.sql` -- type: schema, method: supabase_dashboard_sql, status: applied, depends_on: []
8. `scripts/backfill-pubmed.ts` -- type: backfill, method: tsx_script, status: applied, depends_on: [order 7], post_exec_verification: "SELECT COUNT(*) FROM evidence_links WHERE source='pubmed' AND pmid IS NOT NULL; -- all PubMed rows have pmid"
9. `scripts/backfill-youtube.ts` -- type: backfill, method: tsx_script, status: applied, depends_on: [order 7]

Layer 2 -- Phase 2 dossier-batch (order 10-44, 35 files). Sub-order within Phase 2:
- For each plan group (02-02, 02-03, 02-04, 02-05): source-registry FIRST, then category/product dossiers, then evidence-links, then structured-emission, then taxonomy-additions.
- All Phase 2 files: status: "pending", notes: "12 products show 0 docs in live DB per STATE.md"
- List every file from the compile_sql directory that starts with "02-". There are 35 files total.
- Set pre_exec_checklist for the first file in each plan group to: ["Prior plan group files applied (if any)"]
- Set post_exec_verification for dossier files to: "SELECT COUNT(*) FROM agent_reference_docs WHERE offering_id = 'UUID'; -- expected >= 3"

Layer 3 -- Phase 4 source-ingestion (order 45-46):
45. `supabase/compile_sql/04-01-source-registry-triage.sql` -- type: triage, depends_on: [Phase 2 source-registry files], status: pending
46. `supabase/compile_sql/04-02-evidence-links-fda-urls.sql` -- type: backfill, depends_on: [order 45], status: pending

Layer 4 -- Phase 5 concern-language (order 47-53, 7 files):
47. `05-01-schema-clusters.sql` -- type: schema, depends_on: [], status: pending
48. `05-01-missing-concerns.sql` -- type: data, depends_on: [], status: pending
49. `05-01-execute-phase2-outstanding.sql` -- type: meta, depends_on: [Phase 2 structured-emission files], status: applied, notes: "Contains inline SQL with bug fixes for Phase 2 enum casts; executed 2026-06-13 per file header"
50. `05-02-aliases-aging-volume.sql` -- type: data, depends_on: [order 48], status: pending
51. `05-02-aliases-expression-specialty.sql` -- type: data, depends_on: [order 48], status: pending
52. `05-02-aliases-skin-body.sql` -- type: data, depends_on: [order 48], status: pending
53. `05-03-cluster-definitions.sql` -- type: data, depends_on: [order 47], status: pending

Layer 5 -- Phase 6 pairing-engine (order 54-58, 5 files):
54. `06-00-schema.sql` -- type: schema, depends_on: [], status: pending
55. `06-00-backfill.sql` -- type: backfill, depends_on: [order 54], status: pending
56. `06-02-canonical-common-inserts.sql` -- type: data, depends_on: [order 54], status: "deferred", out_of_sync: true, do_not_execute: true, notes: "WARNING header: OUT OF SYNC with review cards. Must be regenerated in Phase 10. DO NOT EXECUTE.", source_artifact: ".planning/phases/06-pairing-engine/REVIEW_QUEUE/pairings/"
57. `06-02-conditional-compatible-inserts.sql` -- type: data, depends_on: [order 54], status: pending
58. `06-02-do-not-market-inserts.sql` -- type: data, depends_on: [order 54], status: pending

Layer 6 -- Phase 7 timing-rules (order 59-61, 3 files):
59. `07-01-timing-schema.sql` -- type: schema, depends_on: [], status: pending, notes: "Mirror of migrations/20260613_timing_columns.sql; apply migration version preferentially"
60. `07-02-product-cadence.sql` -- type: data, depends_on: [order 59], status: pending, post_exec_verification: "SELECT COUNT(*) FROM gl_products WHERE minimum_retreatment_days IS NOT NULL; -- expected 18"
61. `07-03-pair-timing.sql` -- type: data, depends_on: [order 59, order 56, order 57, order 58], status: "blocked", pre_exec_checklist: ["item_relationships has >= 37 rows with pairing_tier IN ('canonical','common')", "06-02-canonical-common-inserts.sql has been regenerated and applied (Phase 10)"], notes: "Updates timing on pairs; depends on Phase 6 INSERT files being applied first. Will only update conditional/compatible/do-not-market rows until 06-02 canonical-common is resolved."

**Phase 3 note:** Add a comment entry (or a notes-only entry) documenting Phase 3 as code-only (no SQL files). Use type: "no_sql", execution_method: "no_sql", with a note: "Phase 3 produced TypeScript code changes only (lib/retrieval/sources.ts, app/api/research/chat/route.ts). No SQL artifacts."

**EXEC-03 (out-of-sync flagging):** After building all entries, run this grep to confirm you've caught all warning files:
```bash
grep -l "DO NOT EXECUTE\|OUT OF SYNC\|WARNING.*regenerate" supabase/compile_sql/*.sql
```
Every file returned by that grep must have `out_of_sync: true` in its manifest entry.

**Final total_files count:** Sum all entries (should be approximately 61 entries plus the Phase 3 note entry).
  </action>

  <verify>
    <automated>node -e "const m=require('./supabase/EXECUTION_MANIFEST.json'); console.log('entries:', m.entries.length); console.log('has_order:', m.entries.every(e => typeof e.order === 'number')); console.log('out_of_sync:', m.entries.filter(e => e.out_of_sync).map(e => e.file)); console.log('do_not_execute:', m.entries.filter(e => e.do_not_execute).map(e => e.file)); console.log('deferred:', m.entries.filter(e => e.status === 'deferred').map(e => e.file)); console.log('has_pre_exec:', m.entries.filter(e => e.pre_exec_checklist && e.pre_exec_checklist.length > 0).length); console.log('has_post_exec:', m.entries.filter(e => e.post_exec_verification).length);"</automated>
  </verify>

  <acceptance_criteria>
    - supabase/EXECUTION_MANIFEST.json exists and is valid JSON (node -e "require('./supabase/EXECUTION_MANIFEST.json')" exits 0)
    - entries array contains >= 55 entries (52 compile_sql + 6 migrations + 3 scripts - some overlap)
    - Every entry has fields: file, phase, plan, order, type, execution_method, depends_on, status
    - Exactly 1 entry has out_of_sync: true (06-02-canonical-common-inserts.sql)
    - Exactly 1 entry has do_not_execute: true (06-02-canonical-common-inserts.sql)
    - At least 10 entries have non-empty pre_exec_checklist arrays
    - At least 10 entries have non-empty post_exec_verification strings
    - order field values are sequential (no gaps in the critical path)
    - grep "06-02-canonical-common-inserts" supabase/EXECUTION_MANIFEST.json returns lines containing "out_of_sync" and "true"
  </acceptance_criteria>

  <done>
    Single manifest JSON lists all SQL/TS artifacts across phases 01-07 in dependency order. Each entry has status, execution method, dependencies, pre/post checks. Out-of-sync file (06-02-canonical-common-inserts.sql) is flagged with do_not_execute: true. Operator can use this as a runbook.
  </done>
</task>

</tasks>

<verification>
- supabase/EXECUTION_MANIFEST.json is valid JSON
- All 52 compile_sql files appear in the manifest (cross-reference with `ls supabase/compile_sql/*.sql | wc -l`)
- All 6 migration files appear in the manifest
- All 3 Phase 1 script files appear in the manifest
- 06-02-canonical-common-inserts.sql is flagged out_of_sync: true, do_not_execute: true, status: deferred
- grep -l "DO NOT EXECUTE" supabase/compile_sql/*.sql matches the out_of_sync entries
- order field provides a valid topological sort (no entry depends on a higher-ordered entry)
</verification>

<success_criteria>
- EXEC-01: Manifest exists at supabase/EXECUTION_MANIFEST.json listing all files in dependency order with status and phase
- EXEC-02: Every entry has pre_exec_checklist and post_exec_verification fields
- EXEC-03: 06-02-canonical-common-inserts.sql flagged out_of_sync: true with explanation
</success_criteria>

<output>
After completion, create `.planning/phases/08-execution-manifest-and-validation/08-01-SUMMARY.md`
</output>
