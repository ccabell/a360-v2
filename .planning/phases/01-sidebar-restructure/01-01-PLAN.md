---
phase: 01-sidebar-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/collapsible.tsx
  - components/layout/sidebar.tsx
autonomous: true
requirements: [SIDE-01, SIDE-02, SIDE-03, SIDE-04, SIDE-05]

must_haves:
  truths:
    - "Nav items render grouped under the 6 category headers (Imaging ML, Content & Education, Studio & Global Library, Exchange & Agents, Data & RAG, Reserve / Infra) with Overview pinned above the groups"
    - "Clicking a category header collapses/expands that group's items"
    - "On load, the group containing the current active route starts expanded; others start collapsed"
    - "Typing in the filter box narrows visible items by name (case-insensitive substring on item.name); groups with zero matches disappear"
    - "When the filter matches nothing, the nav shows 'No matches' / 'Try a different search term.'"
    - "In demo mode (NEXT_PUBLIC_APP_MODE=demo) scope:internal items are hidden; in internal mode they are visible"
    - "With all groups expanded the nav scrolls independently of the fixed logo header and footer"
  artifacts:
    - path: "components/ui/collapsible.tsx"
      provides: "shadcn Collapsible primitive (Collapsible / CollapsibleTrigger / CollapsibleContent) wrapping @base-ui/react"
      contains: "export"
    - path: "components/layout/sidebar.tsx"
      provides: "Grouped, collapsible, filterable sidebar preserving the demo/internal split and existing active/hover tokens"
      min_lines: 150
  key_links:
    - from: "components/layout/sidebar.tsx"
      to: "components/ui/collapsible.tsx"
      via: "import Collapsible/CollapsibleContent"
      pattern: "@/components/ui/collapsible"
    - from: "components/layout/sidebar.tsx"
      to: "components/portfolio/display.ts"
      via: "import CATEGORY_ORDER for group order + labels"
      pattern: "CATEGORY_ORDER"
    - from: "components/layout/sidebar.tsx"
      to: "NEXT_PUBLIC_APP_MODE scope filter"
      via: "isDemo filter applied before grouping"
      pattern: "scope"
---

<objective>
Refactor the flat 22-item `/dashboard` sidebar (`components/layout/sidebar.tsx`) into grouped, collapsible, filterable navigation that scales past 20 entries — WITHOUT breaking the demo/internal split. This is a STRUCTURAL refactor, not a restyle: every color/spacing/typography token and the active/hover styling is preserved verbatim from the current component and the authoritative `01-UI-SPEC.md`.

Purpose: The sidebar is Chris's daily runtime and the project count keeps growing; a flat 22-item list is already unwieldy and unscrollable. Grouping into the Command Center's existing 6-category taxonomy plus a filter box keeps it usable at 20+ entries.

Output:
- `components/ui/collapsible.tsx` — new shadcn primitive (added via CLI).
- `components/layout/sidebar.tsx` — grouped collapsible groups (SIDE-01), filter box (SIDE-02), preserved demo/internal split (SIDE-03), collapse/expand + active-route auto-expand (SIDE-04), independent scroll for 20+ entries (SIDE-05).

Covers requirements: SIDE-01, SIDE-02, SIDE-03, SIDE-04, SIDE-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sidebar-restructure/01-UI-SPEC.md

# The component being refactored and the taxonomy source
@components/layout/sidebar.tsx
@components/portfolio/display.ts

<interfaces>
<!-- Contracts the executor needs — no codebase exploration required. -->

From components/portfolio/display.ts (import and use for group order + exact header labels):

  export const CATEGORY_ORDER = [
    "Imaging ML",
    "Content & Education",
    "Studio & Global Library",
    "Exchange & Agents",
    "Data & RAG",
    "Reserve / Infra",
  ];

Existing scope/demo split in sidebar.tsx (PRESERVE EXACTLY — do not change the semantics):

  const APP_MODE = process.env.NEXT_PUBLIC_APP_MODE ?? "internal";
  const isDemo = APP_MODE === "demo";
  // applied as:
  const items = menuItems.filter(
    (i) => !isDemo || !("scope" in i && i.scope === "internal"),
  );

Existing active-route detection (PRESERVE EXACTLY):

  const isActive =
    item.href === "/dashboard"
      ? pathname === "/dashboard"
      : pathname === item.href || pathname.startsWith(item.href + "/");

Existing active/inactive row classes (PRESERVE VERBATIM — copy these strings, do not rewrite):

  row base : w-full px-4 py-2.5 rounded-lg flex items-center gap-3 transition-all text-sm font-medium
  active   : bg-sidebar-primary/10 text-sidebar-primary border border-sidebar-primary/30
  inactive : text-sidebar-foreground hover:bg-sidebar-primary/5
  icon active   : text-sidebar-primary
  icon inactive : text-sidebar-foreground/60
  active dot    : w-1.5 h-1.5 bg-sidebar-primary rounded-full (div)

New shadcn Collapsible (added in Task 1) — used in CONTROLLED mode by Tasks 2/3:

  import { Collapsible, CollapsibleContent } from "@/components/ui/collapsible";
  // <Collapsible open={isOpen} onOpenChange={setIsOpen}> header-trigger + <CollapsibleContent>{rows}</CollapsibleContent> </Collapsible>
  // Expansion is driven by React state (needed for active-route auto-expand + filter override),
  // so drive the ChevronRight rotate-90 from that same isOpen state, not a data-attribute.
</interfaces>

<spec_note>
The item->category mapping in 01-UI-SPEC.md is an APPROVED assumption (no per-item category field exists in the DB). Implement it exactly as given below. If any single placement looks obviously wrong during execution, note it in the SUMMARY for Chris — do NOT block or silently re-map.
</spec_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the shadcn collapsible primitive</name>
  <read_first>
    - components/layout/sidebar.tsx
    - .planning/phases/01-sidebar-restructure/01-UI-SPEC.md (Design System + Registry Safety sections)
    - components.json
  </read_first>
  <files>components/ui/collapsible.tsx</files>
  <action>
Add the official shadcn `collapsible` primitive so Task 2 can import it. It does not exist yet (`components/ui/collapsible.tsx` is absent; `input.tsx` already exists and is reused as-is in Task 3).

Run from project root `C:/Projects/a360-v2` as cwd:

  npx shadcn@latest add collapsible --yes

This project's preset is `base-nova` (`components.json` present) and wraps `@base-ui/react@^1.5.0` — the CLI resolves the correct Base UI variant automatically. Do NOT hand-write the primitive and do NOT add any third-party registry (Registry Safety in the UI-SPEC: official registry only).

If the CLI errors or writes to an unexpected path, do NOT retry blindly — read the generated file, confirm it exports `Collapsible`, `CollapsibleTrigger`, and `CollapsibleContent`, and that it lives at `components/ui/collapsible.tsx`. If the CLI cannot run non-interactively in this environment, fall back to copying the official Base UI `collapsible` source for the installed `@base-ui/react@1.5.0` into `components/ui/collapsible.tsx` with the same three exports, then note the fallback in the SUMMARY.

Do NOT modify `sidebar.tsx` in this task.
  </action>
  <verify>
    <automated>cd /c/Projects/a360-v2 && test -f components/ui/collapsible.tsx && rg -q "Collapsible" components/ui/collapsible.tsx && rg -q "CollapsibleContent" components/ui/collapsible.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/ui/collapsible.tsx` succeeds (file exists).
    - `rg "export" components/ui/collapsible.tsx` shows `Collapsible` and `CollapsibleContent` exported.
    - `git diff --stat components/layout/sidebar.tsx` shows NO change (sidebar untouched this task).
    - `components.json` gained no third-party registry entry (`rg -i "registries" components.json` shows none added).
  </acceptance_criteria>
  <done>The Collapsible primitive exists at `components/ui/collapsible.tsx` with the three standard exports and is importable via `@/components/ui/collapsible`.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor sidebar into collapsible category groups (SIDE-01, SIDE-03, SIDE-04, SIDE-05)</name>
  <read_first>
    - components/layout/sidebar.tsx
    - components/portfolio/display.ts
    - components/ui/collapsible.tsx
    - .planning/phases/01-sidebar-restructure/01-UI-SPEC.md (Component Inventory and Interaction Contract section)
  </read_first>
  <files>components/layout/sidebar.tsx</files>
  <action>
Restructure the render from a flat `items.map(...)` into CATEGORY_ORDER-driven collapsible groups with "Overview" pinned above. Keep the logo header and footer exactly as-is.

1. ADD a `category` field to each `menuItems` entry (except pinned "Overview") using the EXACT strings from `CATEGORY_ORDER` (import `{ CATEGORY_ORDER }` from `@/components/portfolio/display`). Group order AND header labels both come from `CATEGORY_ORDER` — do not retype the strings independently. Assign per the approved UI-SPEC mapping:
   - Overview (`/dashboard`) -> PINNED, no category (rendered above the groups).
   - Imaging ML: Age Progression (`/dashboard/age-progression`, internal), Before After (`/dashboard/before-after`, internal).
   - Content & Education: Library (`/dashboard/library`, demo), Pearce Channel (`/dashboard/academy`, demo), Video Navigator (`/tube`, demo), Podcast Navigator (`/podcast`, demo), LPOA (`/dashboard/lpoa`, internal).
   - Studio & Global Library: TCP (`/dashboard/tcp`, demo), Global Library (`/dashboard/global-library`, internal), Studio (`/dashboard/studio`, internal).
   - Exchange & Agents: Scribe (`/dashboard/scribe`, demo), Reach (`/dashboard/reach`, demo), Intelligence (`/dashboard/intelligence`, demo), Agent Tester (`/dashboard/agent-tester`, internal), History (`/dashboard/history`, internal), Agent Runs (`/dashboard/agent-runs`, internal), Agent Manager (`/dashboard/agents`, internal).
   - Data & RAG: Products (`/dashboard/products`, demo), Patients (`/dashboard/patients`, internal), Fuel Library (`/dashboard/fuel-library`, internal).
   - Reserve / Infra: Portfolio (`/dashboard/portfolio`, internal).
   Keep every existing `href`, `icon`, and `scope` value unchanged — you are ONLY adding a `category` field. Do NOT change which items carry `scope: "internal"`.

2. PRESERVE the demo/internal split EXACTLY (SIDE-03). Keep `APP_MODE`/`isDemo` and the filter `menuItems.filter((i) => !isDemo || !("scope" in i && i.scope === "internal"))` verbatim. Apply this scope filter FIRST, then group the surviving items by `category`. Internal tools must never render in demo mode.

3. RENDER pinned Overview above the groups using the SAME row markup/classes as today (base row `w-full px-4 py-2.5 rounded-lg flex items-center gap-3 transition-all text-sm font-medium`, active/inactive branches verbatim, active dot verbatim, wrapped in `next/link`). Reuse the existing `isActive` logic unchanged.

4. RENDER one Collapsible group per category, iterating in `CATEGORY_ORDER` order. Skip a category entirely if its scope-filtered item list is empty (e.g. in demo mode "Imaging ML" and "Reserve / Infra" have no demo items). For each group:
   - Header row = a button trigger: lucide `ChevronRight` that rotates to open via `rotate-90` when the group's `open` boolean is true + category label styled `text-xs font-medium uppercase tracking-wide text-sidebar-foreground/60`, header vertical padding `py-2`, horizontal `px-4`. No count badge. Header hover uses `hover:bg-sidebar-primary/5`. Chevron/label resting color is neutral `text-sidebar-foreground/60` — NEVER the accent color.
   - Body = `CollapsibleContent` containing that category's item rows, each rendered with the SAME row markup/classes as Overview (active/inactive branches verbatim), `space-y-1` between rows.
   - Use the Collapsible in CONTROLLED mode: `open={openGroups[category]}` with an `onOpenChange` that toggles that category in a `useState` map. Multiple groups may be open at once (accordion-all, not single).

5. ACTIVE-ROUTE auto-expand on load (SIDE-04). Initialize the open-state map so the group containing the current active route (`pathname` match using the existing `isActive` logic) starts `open`, and all other groups start collapsed. If the active route is the pinned "Overview", all groups start collapsed. Derive this from `pathname`.

6. INDEPENDENT scroll for 20+ entries (SIDE-05). Add `overflow-y-auto` to the `<nav>` container (currently `flex-1 p-4 space-y-1`). Keep the logo header and footer fixed; add `shrink-0` to them if needed so only the nav scrolls. Sidebar width stays `w-64`. No icon-rail/responsive-collapse behavior.

Do NOT restyle nav rows, change `py-2.5`, alter any `--sidebar-*` token usage, or touch the footer's Sign out/Settings logic. The filter Input is added in Task 3 — leave a clear insertion point directly below the logo header for it, but do not add filter logic yet.
  </action>
  <verify>
    <automated>cd /c/Projects/a360-v2 && rg -q "@/components/ui/collapsible" components/layout/sidebar.tsx && rg -q "CATEGORY_ORDER" components/layout/sidebar.tsx && rg -q "overflow-y-auto" components/layout/sidebar.tsx && rg -q "scope" components/layout/sidebar.tsx && rg -q "bg-sidebar-primary/10 text-sidebar-primary border border-sidebar-primary/30" components/layout/sidebar.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Imports Collapsible: `rg "@/components/ui/collapsible" components/layout/sidebar.tsx` matches.
    - Uses the taxonomy source: `rg "CATEGORY_ORDER" components/layout/sidebar.tsx` matches (imported from `@/components/portfolio/display`).
    - Demo/internal split intact (SIDE-03 hard constraint): `rg "isDemo" components/layout/sidebar.tsx` matches AND the scope-filter expression `i.scope === "internal"` is still present.
    - Active-item classes preserved verbatim: `rg "bg-sidebar-primary/10 text-sidebar-primary border border-sidebar-primary/30" components/layout/sidebar.tsx` matches.
    - Scroll added (SIDE-05): `rg "overflow-y-auto" components/layout/sidebar.tsx` matches.
    - No mis-spelled category label: `rg "Reserve/Infra" components/layout/sidebar.tsx` returns NO match (canonical is `Reserve / Infra` with spaces, sourced from CATEGORY_ORDER).
    - The pre-existing `py-2.5` row geometry is unchanged: `rg "py-2.5" components/layout/sidebar.tsx` matches.
    - Types compile: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Sidebar renders Overview pinned above 6 collapsible category groups in CATEGORY_ORDER; the active route's group is expanded on load; demo mode hides internal items; the nav scrolls independently; `npx tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Add the filter box and filtering behavior (SIDE-02)</name>
  <read_first>
    - components/layout/sidebar.tsx
    - components/ui/input.tsx
    - .planning/phases/01-sidebar-restructure/01-UI-SPEC.md (Copywriting Contract + Filter/search behavior sections)
  </read_first>
  <files>components/layout/sidebar.tsx</files>
  <action>
Add a client-side filter box directly below the logo header (the insertion point left in Task 2), above the pinned Overview item and the category groups. No network request — pure filtering over the static `menuItems`.

1. FILTER INPUT: one `Input` (existing `@/components/ui/input`, reused as-is at default sizing) wrapped in a `relative` container. Add a lucide `Search` icon at `size-3.5`, `text-sidebar-foreground/40`, absolutely positioned `left-2.5` (vertically centered), and give the input `pl-8` to clear it. Use these EXACT strings (from the UI-SPEC Copywriting Contract, verbatim — do not paraphrase):
   - placeholder = `Search tools...`
   - aria-label = `Filter sidebar navigation`
   Back the input with a `useState<string>("")` query value.

2. FILTERING: case-insensitive substring match against `item.name` ONLY (not `href`). Apply AFTER the existing scope filter. When the query is non-empty:
   - Hide any item whose name does not contain the query (case-insensitive, e.g. compare `name.toLowerCase().includes(query.toLowerCase())`).
   - Hide the pinned "Overview" too if its name does not match (it is NOT exempt from the filter).
   - Hide any category group that has zero matching items (do not render it collapsed-empty).
   - Every group that still has at least one match auto-expands, OVERRIDING the on-load active-route expansion (so matches are always visible without a manual expand).

3. EMPTY STATE: if the non-empty query matches nothing (no Overview, no group items), render in place of the group list — the EXACT copy from the UI-SPEC:
   - heading line `No matches`
   - body line `Try a different search term.`
   styled `text-sm text-sidebar-foreground/60`, centered, `py-8`.

4. CLEARING the filter (query back to empty string) RESTORES the on-load expand state from Task 2 (active route's group open, others collapsed) — do not leave whatever expand state the filter produced. Implement by making the effective open-state conditional: query non-empty -> groups-with-matches are open; query empty -> fall back to the Task 2 active-route initial map.

Keep everything from Task 2 intact — do not change the group rendering, tokens, scope filter, or scroll behavior. Only add the filter input and the query-driven visibility/expansion layer on top.
  </action>
  <verify>
    <automated>cd /c/Projects/a360-v2 && rg -q "Search tools" components/layout/sidebar.tsx && rg -q "Filter sidebar navigation" components/layout/sidebar.tsx && rg -q "No matches" components/layout/sidebar.tsx && rg -q "Try a different search term" components/layout/sidebar.tsx && rg -q "toLowerCase" components/layout/sidebar.tsx && rg -q "@/components/ui/input" components/layout/sidebar.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Filter input present with exact copy: `rg "Search tools" components/layout/sidebar.tsx` and `rg "Filter sidebar navigation" components/layout/sidebar.tsx` both match.
    - Input component reused: `rg "@/components/ui/input" components/layout/sidebar.tsx` matches.
    - Case-insensitive name match implemented: `rg "toLowerCase" components/layout/sidebar.tsx` matches.
    - Empty-state copy exact: `rg "No matches" components/layout/sidebar.tsx` and `rg "Try a different search term" components/layout/sidebar.tsx` both match.
    - SIDE-03 still intact after adding the filter: `rg "isDemo" components/layout/sidebar.tsx` still matches and the scope filter is applied before the name filter.
    - Types compile: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Typing in the filter narrows visible items by name, hides empty groups, auto-expands matching groups, shows the exact empty-state copy on no match, and restores active-route expansion when cleared; `npx tsc --noEmit` passes.</done>
</task>

</tasks>

<verification>
Phase-level checks (run after all three tasks):

1. Build compiles under the installed Next 16.2.9 / React 19:
   `cd /c/Projects/a360-v2 && npm run build` completes without type or lint errors touching `sidebar.tsx`/`collapsible.tsx`.
2. Demo/internal split (SIDE-03) proven both ways in dev:
   - `NEXT_PUBLIC_APP_MODE=demo npm run dev` -> internal-only groups/items (Imaging ML, Reserve / Infra, Portfolio, Patients, etc.) are ABSENT; demo tools (Scribe, Library, Products...) are present.
   - default/internal mode -> all 22 items present across the 6 groups.
3. Interaction smoke (local `/dashboard`): category headers collapse/expand; loading a route inside a group (e.g. `/dashboard/agents`) shows that group expanded on load; typing `age` in the filter shows only Age Progression and expands its group; typing `zzz` shows `No matches` / `Try a different search term.`; clearing restores active-route expansion.
4. Scroll: with all groups expanded in internal mode, the nav area scrolls while the A360 logo header and the Sign out/Settings footer stay fixed.
</verification>

<success_criteria>
- SIDE-01: 22 items render grouped under the 6 `CATEGORY_ORDER` headers with Overview pinned above.
- SIDE-02: filter box narrows visible items by name; empty-state copy shows on no match.
- SIDE-03: demo mode hides `scope:"internal"` items; internal mode shows them (scope filter byte-for-byte preserved).
- SIDE-04: category groups collapse/expand; the active route's group is expanded on load.
- SIDE-05: `overflow-y-auto` on the nav keeps the sidebar usable at 20+ entries with header/footer fixed.
- `npm run build` passes; all active/hover/token classes preserved verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sidebar-restructure/01-01-SUMMARY.md` per the summary template. Note in it: (a) confirmation the item->category mapping matched the UI-SPEC (or any placement Chris should re-check), (b) whether `collapsible` came from the shadcn CLI or the documented fallback, and (c) the results of the demo-vs-internal dev verification.
</output>
