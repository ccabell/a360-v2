---
phase: 02-dossier-batch
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - compile_manifest.json
  - REVIEW_QUEUE/
autonomous: true
requirements: []

must_haves:
  truths:
    - "Sculptra, Radiesse, Kybella, and CoolSculpting each have 5 agent_reference_docs rows (status=draft)"
    - "Each product has item_concerns, item_body_areas, aliases, and does_not_solve populated"
    - "Biostimulator products inherit from the Biostimulators category via extends_doc_id"
    - "Body contouring products inherit from the Body Contouring category via extends_doc_id"
  artifacts:
    - path: "compile_manifest.json"
      provides: "Updated with biostimulator + body contouring product compile status"
  key_links:
    - from: "agent_reference_docs (Sculptra/Radiesse)"
      to: "agent_reference_docs (Biostimulators category)"
      via: "extends_doc_id"
      pattern: "extends_doc_id"
    - from: "agent_reference_docs (Kybella/CoolSculpting)"
      to: "agent_reference_docs (Body Contouring category)"
      via: "extends_doc_id"
      pattern: "extends_doc_id"
---

<objective>
Compile product dossiers + structured emission for Biostimulators (Sculptra, Radiesse) and Body Contouring (Kybella, CoolSculpting) products.

Purpose: These are distinct product categories with different mechanisms (collagen stimulation vs fat reduction). Smaller product groups but clinically important for the demo.

Output: ~20 agent_reference_docs rows, item_concerns/item_body_areas/aliases/does_not_solve rows, source capture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dossier-batch/STRUCTURED_EMISSION_ADDENDUM.md
@.planning/phases/02-dossier-batch/BATCH_SOURCE_LOGGING_ADDENDUM.md
@.planning/phases/02-dossier-batch/AESTHETIC_DERMATOLOGY_JOURNAL_REGISTRY.md
@Fable Docs/DOSSIER_COMPILE_PIPELINE.md
@Fable Docs/DOSSIER_TEMPLATES.md
@compile_manifest.json
@.planning/phases/02-dossier-batch/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile Biostimulator products — Sculptra + Radiesse (+ Radiesse(+) if exists)</name>
  <files>compile_manifest.json, REVIEW_QUEUE/</files>
  <read_first>
    - compile_manifest.json (get product IDs)
    - .planning/phases/02-dossier-batch/02-02-SUMMARY.md (Biostimulators category doc IDs for extends_doc_id)
    - Fable Docs/DOSSIER_COMPILE_PIPELINE.md
    - Fable Docs/DOSSIER_TEMPLATES.md
    - .planning/phases/02-dossier-batch/STRUCTURED_EMISSION_ADDENDUM.md
    - .planning/phases/02-dossier-batch/BATCH_SOURCE_LOGGING_ADDENDUM.md
    - .planning/phases/02-dossier-batch/AESTHETIC_DERMATOLOGY_JOURNAL_REGISTRY.md
  </read_first>
  <action>
    For EACH biostimulator product (Sculptra, Radiesse, Radiesse(+) if in DB):

    **Dossier compile (Steps 1-5 per DOSSIER_COMPILE_PIPELINE.md):**
    - Set extends_doc_id to Biostimulators category docs. Write BRAND DELTA only.
    - Biostimulators are collagen-stimulating — the clinical lens should emphasize delayed onset, progressive results over months, and the mechanism distinction from fillers (collagen induction vs volumization).
    - sales_education PRIMARY: benefit_framing around "your own collagen" narrative, maintenance_story around multi-session protocols, cost_benefit around long-term value.

    **Structured emission:**

    a) item_concerns:
       - Sculptra (PLLA): Facial Volume Loss (primary, FDA-indicated for facial lipoatrophy), Skin Laxity (secondary), Temple Hollowing (primary), Cheek Volume Loss (primary), Jawline Definition (adjunctive), Buttock Augmentation (off-label, is_fda_indicated=false)
       - Radiesse (CaHA): Nasolabial Folds (primary, FDA-indicated), Cheek Volume Loss (primary, FDA-indicated), Hand Rejuvenation (primary, FDA-indicated), Jawline Definition (secondary), Marionette Lines (primary)
       - Radiesse(+) (dilute/hyperdilute): Skin Quality (primary), Skin Laxity (secondary), Biostimulation (primary)
       Exact concerns depend on what exists in the concerns table — verify and add if missing.

    b) item_body_areas: zone-level — temples, cheeks, jawline, hands (Radiesse), nasolabial folds, etc.

    c) aliases: "my face looks hollow" -> Facial Volume Loss, "gaunt cheeks" -> Cheek Volume Loss, "bony hands" -> Hand Volume Loss, "saggy jawline" -> Jawline Definition, etc.

    d) does_not_solve:
       - Sculptra: ["dynamic expression lines", "immediate volume correction", "lip augmentation", "fine surface lines", "pigmentation", "vascular concerns"]
       - Radiesse: ["dynamic expression lines", "lip augmentation (contraindicated)", "fine perioral lines", "skin texture at surface level", "pigmentation"]

    **Source capture:** Log biostimulator research sources. Aesthetic Plastic Surgery and ASJ publish significant biostimulator studies.

    **Update compile_manifest.json, write REVIEW_QUEUE files.**
  </action>
  <verify>
    <automated>
    Run against Supabase:
    SELECT p.name, COUNT(ard.id) as docs,
      (SELECT COUNT(*) FROM item_concerns ic WHERE ic.offering_id=p.id) as concerns,
      (SELECT COUNT(*) FROM item_body_areas iba WHERE iba.offering_id=p.id) as body_areas,
      COALESCE(array_length(p.does_not_solve,1),0) as limitations
    FROM products p
    JOIN agent_reference_docs ard ON ard.offering_id = p.id AND ard.status='draft'
    WHERE p.name ILIKE '%sculptra%' OR p.name ILIKE '%radiesse%'
    GROUP BY p.id, p.name, p.does_not_solve;
    -- expect: each has docs=5, concerns>=3, body_areas>=1, limitations>=4
    </automated>
  </verify>
  <acceptance_criteria>
    - Sculptra and Radiesse each have 5 agent_reference_docs rows with status='draft'
    - extends_doc_id points to Biostimulators category docs
    - Each has >= 4 item_concerns rows
    - Each has item_body_areas at zone/area level
    - Each has does_not_solve with >= 5 entries
    - Radiesse lip augmentation noted as contraindicated in does_not_solve
    - source_registry has new biostimulator research sources
  </acceptance_criteria>
  <done>Biostimulator products compiled with full dossiers and structured intelligence</done>
</task>

<task type="auto">
  <name>Task 2: Compile Body Contouring products — Kybella + CoolSculpting (+ any others in DB)</name>
  <files>compile_manifest.json, REVIEW_QUEUE/</files>
  <read_first>
    - compile_manifest.json (get product IDs)
    - .planning/phases/02-dossier-batch/02-02-SUMMARY.md (Body Contouring category doc IDs)
    - Fable Docs/DOSSIER_COMPILE_PIPELINE.md
    - Fable Docs/DOSSIER_TEMPLATES.md
    - .planning/phases/02-dossier-batch/STRUCTURED_EMISSION_ADDENDUM.md
    - .planning/phases/02-dossier-batch/BATCH_SOURCE_LOGGING_ADDENDUM.md
  </read_first>
  <action>
    For EACH body contouring product (Kybella, CoolSculpting, others in DB):

    **Dossier compile:** Set extends_doc_id to Body Contouring category docs. Write BRAND DELTA.
    - Kybella (deoxycholic acid): injectable, submental fat (FDA-indicated). Very narrow indication.
    - CoolSculpting (cryolipolysis): non-invasive, multiple body areas (FDA-cleared). Device-based.

    **Structured emission:**

    a) item_concerns:
       - Kybella: Submental Fullness (primary, FDA-indicated), Double Chin (alias->Submental Fullness)
       - CoolSculpting: Submental Fullness (primary, FDA-cleared), Flank Fat (primary, FDA-cleared), Abdominal Fat (primary, FDA-cleared), Thigh Fat (primary), Bra Fat (primary), Back Fat (primary), Upper Arm Fat (primary), Banana Roll (secondary)

    b) item_body_areas:
       - Kybella: Submental (zone), very narrow
       - CoolSculpting: Abdomen, Flanks, Inner Thighs, Outer Thighs, Upper Arms, Back, Submental, Bra Fat area

    c) aliases: "double chin" -> Submental Fullness, "love handles" -> Flank Fat, "muffin top" -> Abdominal Fat, "bra bulge" -> Bra Fat, "arm flab" -> Upper Arm Fat, etc.

    d) does_not_solve:
       - Kybella: ["skin laxity", "body fat beyond submental", "facial volume", "muscle-related jawline", "weight loss"]
       - CoolSculpting: ["skin laxity", "visceral fat", "weight loss", "muscle tone", "cellulite", "loose skin after fat removal"]

    NOTE: CoolSculpting has a known rare side effect (PAH — paradoxical adipose hyperplasia) that MUST appear in the clinical safety section. This is a safety floor item.

    **Source capture + taxonomy audit + compile_manifest update + REVIEW_QUEUE files.**
  </action>
  <verify>
    <automated>
    Run against Supabase:
    SELECT p.name, COUNT(ard.id) as docs,
      (SELECT COUNT(*) FROM item_concerns ic WHERE ic.offering_id=p.id) as concerns,
      COALESCE(array_length(p.does_not_solve,1),0) as limitations
    FROM products p
    JOIN agent_reference_docs ard ON ard.offering_id = p.id AND ard.status='draft'
    WHERE p.name ILIKE '%kybella%' OR p.name ILIKE '%coolsculpting%'
    GROUP BY p.id, p.name, p.does_not_solve;
    -- expect: each has docs=5, concerns>=1 (Kybella narrow), limitations>=4
    </automated>
  </verify>
  <acceptance_criteria>
    - Kybella and CoolSculpting each have 5 agent_reference_docs rows with status='draft'
    - extends_doc_id points to Body Contouring category docs
    - Kybella has >= 1 item_concerns (narrow FDA indication)
    - CoolSculpting has >= 5 item_concerns (multiple cleared areas)
    - CoolSculpting clinical_summary mentions PAH risk in safety section
    - Each has does_not_solve populated
    - aliases table has body contouring patient language
  </acceptance_criteria>
  <done>Body contouring products compiled with full dossiers and structured intelligence</done>
</task>

</tasks>

<verification>
```sql
SELECT p.name,
  (SELECT COUNT(*) FROM agent_reference_docs ard WHERE ard.offering_id=p.id AND ard.status='draft') AS docs,
  (SELECT COUNT(*) FROM item_concerns ic WHERE ic.offering_id=p.id) AS concerns,
  (SELECT COUNT(*) FROM item_body_areas iba WHERE iba.offering_id=p.id) AS body_areas,
  COALESCE(array_length(p.does_not_solve,1),0) AS limitations
FROM products p
WHERE p.name ILIKE ANY(ARRAY['%sculptra%','%radiesse%','%kybella%','%coolsculpting%'])
ORDER BY p.name;
-- Expect: no zeros in docs or limitations
```
</verification>

<success_criteria>
All biostimulator and body contouring products have complete dossiers, structured intelligence, and source capture. These are smaller product groups but clinically distinct, requiring different framing in sales_education.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dossier-batch/02-04-SUMMARY.md`
</output>
