---
phase: 03-retrieval-wiring
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "An unscripted Botox/Neurotoxins question in the live UI renders a grounded prose response"
    - "The response shows >=1 clickable 'View on PubMed' link to pubmed.ncbi.nlm.nih.gov"
    - "The response shows >=1 clickable 'Open FDA label' link to accessdata.fda.gov"
    - "Citation chips [1][2] in the prose map to the matching numbered cards in the references panel"
  artifacts: []
  key_links:
    - from: "/dashboard/research (live UI)"
      to: "/api/research/chat"
      via: "fetch POST observed in devtools Network"
      pattern: "/api/research/chat"
---

<objective>
Prove the demo end-to-end in the live UI. This is the acceptance gate for the phase: a fresh, unscripted Botox/Neurotoxins question typed into `/dashboard/research` must render grounded prose with working PubMed + FDA citations from the real DB, and the inline chips must map to the source cards.

Purpose: SC-2, SC-3, SC-4, SC-5 are visual/interactive and cannot be fully proven by grep — they require a human looking at the rendered tab with real data and real LLM output. This checkpoint closes them.
Output: A human-confirmed demo run (or a list of defects to feed `/gsd:plan-phase --gaps`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-retrieval-wiring/03-RESEARCH.md
@.planning/phases/03-retrieval-wiring/03-01-SUMMARY.md
@.planning/phases/03-retrieval-wiring/03-02-SUMMARY.md
@.planning/phases/03-retrieval-wiring/03-03-SUMMARY.md
@components/grounding/citation-card.tsx
@lib/retrieval/locator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight automated smoke (route returns real pubmed + fda urls)</name>
  <files>(smoke test only — no files modified)</files>
  <read_first>
    - .planning/phases/03-retrieval-wiring/03-02-SUMMARY.md (route path + sample curl excerpt)
    - app/api/research/chat/route.ts (the route under test)
    - .env.local (confirm AI_GATEWAY_API_KEY present — from 03-01)
  </read_first>
  <action>
    Start the dev server (`npm run dev`) in the background if not already running. Confirm `AI_GATEWAY_API_KEY` is set in `.env.local` (from Plan 01); if absent, stop and tell the human to provision it — generation will 500 without it.

    Run a smoke curl against the live route:
    ```bash
    curl -N -X POST http://localhost:3000/api/research/chat \
      -H "Content-Type: application/json" \
      -d '{"query":"How fast does Botox work and what is the glabellar dose?"}'
    ```
    Capture the streamed `data:` lines. Assert in the output:
    - a `"type":"sources"` event whose JSON contains `pubmed.ncbi.nlm.nih.gov`
    - the same or another event payload contains `accessdata.fda.gov`
    - at least one `"type":"token"` event (non-empty prose streamed)
    - a `"type":"citations"` event with a non-empty `citations` array
    - a terminating `"type":"done"` event

    If FDA url is absent, check 03-01-SUMMARY: Phase 1 FDA backfill may not have run. Record this as a data gap (not a code defect) for the human.
  </action>
  <verify>
    <automated>curl -s -N -X POST http://localhost:3000/api/research/chat -H "Content-Type: application/json" -d "{\"query\":\"How fast does Botox work and what is the glabellar dose?\"}" | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const fail=(m)=>{console.error('FAIL:',m);process.exit(1)}; if(!d.includes('pubmed.ncbi.nlm.nih.gov'))fail('no pubmed url in stream'); if(!d.includes('\"type\":\"token\"'))fail('no token events'); if(!d.includes('\"type\":\"done\"'))fail('no done event'); if(!d.includes('accessdata.fda.gov'))console.error('WARN: no accessdata.fda.gov url — check Phase 1 FDA backfill'); console.log('smoke ok (fda warn non-fatal, confirm in UI)')})"</automated>
  </verify>
  <acceptance_criteria>
    - The route streams a `sources` event whose payload contains `pubmed.ncbi.nlm.nih.gov`
    - The route streams >=1 `token` event and a terminating `done` event
    - Presence/absence of `accessdata.fda.gov` is recorded (absence flagged as a Phase 1 data gap, not a code defect)
  </acceptance_criteria>
  <done>The live route returns a streamed, cited response containing a real PubMed url; FDA url presence recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UI demo — unscripted Botox question (SC-2 through SC-5)</name>
  <files>(human verification only — no files modified)</files>
  <what-built>
    The Research/Evidence tab is fully wired to the real retrieval route. Claude has confirmed the route streams real PubMed (and ideally FDA) urls. The human now verifies the rendered experience: prose, clickable citations, and chip-to-card mapping.
  </what-built>
  <read_first>
    - components/grounding/citation-card.tsx (renders the "View on PubMed" / "Open FDA label" deep links)
    - lib/retrieval/locator.ts (deepLinkLabel: "View on PubMed", "Open FDA label · p.N")
    - .planning/phases/03-retrieval-wiring/03-02-SUMMARY.md
  </read_first>
  <how-to-verify>
    1. Ensure the dev server is running (`npm run dev`) and `AI_GATEWAY_API_KEY` is set in `.env.local`.
    2. Open http://localhost:3000/dashboard/research in a browser.
    3. Confirm the header badge reads "Live" (not "Demo data").
    4. Type an UNSCRIPTED Botox/Neurotoxins question (not an example button) — e.g. "What's the recommended glabellar Botox dose and how soon does it work?" — and submit.
    5. SC-2: A grounded prose answer streams in (status indicator -> source pills -> streaming text -> reference cards). The prose is non-empty and on-topic.
    6. SC-3: At least one reference card shows a "View on PubMed" link. Click it — it opens `https://pubmed.ncbi.nlm.nih.gov/...` in a new tab.
    7. SC-4: At least one reference card shows an "Open FDA label" link pointing to `accessdata.fda.gov`. Click it — the FDA PDF opens. (If absent, confirm whether Phase 1 FDA backfill has run — this is a data dependency, not a UI defect.)
    8. SC-5: Pick a `[1]` (or any numbered chip) in the prose; clicking it (or the matching source pill) scrolls to reference card #1, and the numbers match — chip [n] corresponds to the card labeled n.
    9. Confirm in devtools Network tab that a POST to `/api/research/chat` fired (proves no mock).
    10. Reply "approved" if SC-2-SC-5 all pass, or list exactly which criterion failed and what you saw (feeds `/gsd:plan-phase --gaps`).
  </how-to-verify>
  <action>
    Human-driven verification (no code). Claude ensures the dev server is running and AI_GATEWAY_API_KEY is set, opens /dashboard/research, and walks the human through the how-to-verify steps. Collect the human verdict per SC-2 through SC-5.
  </action>
  <verify>
    <automated>MISSING — manual checkpoint. Automated coverage is the smoke test in Task 1; SC-2/SC-3/SC-4/SC-5 are visual/interactive and human-confirmed here.</automated>
  </verify>
  <done>Human types "approved" confirming SC-2 through SC-5 pass in the live UI, or lists defects for /gsd:plan-phase --gaps.</done>
  <acceptance_criteria>
    - Header badge reads "Live"
    - An unscripted Botox question produces non-empty grounded prose (SC-2)
    - >=1 reference card links to pubmed.ncbi.nlm.nih.gov and the link opens (SC-3)
    - >=1 reference card links to accessdata.fda.gov and opens — OR FDA-data-gap explicitly recorded against Phase 1 (SC-4)
    - Inline numbered chips map to the correspondingly numbered reference cards (SC-5)
    - A POST to /api/research/chat is observed in the Network tab (confirms live path, SC-1/SC-6)
  </acceptance_criteria>
  <resume-signal>Type "approved" if all of SC-2 through SC-5 pass in the live UI, or list which criterion failed and exactly what you observed.</resume-signal>
</task>

</tasks>

<verification>
- Automated smoke confirms the route streams a real pubmed url + token + done events.
- Human confirms the live UI renders grounded prose with clickable PubMed + FDA citations and correct chip-to-card mapping.
</verification>

<success_criteria>
- SC-2: unscripted Botox/Neurotoxins question renders grounded prose (human-confirmed).
- SC-3: clickable PubMed link to pubmed.ncbi.nlm.nih.gov (human-confirmed).
- SC-4: clickable FDA label link to accessdata.fda.gov (human-confirmed, or data gap recorded).
- SC-5: citation chips [1][2] map to source panel entries (human-confirmed).
</success_criteria>

<output>
After completion, create `.planning/phases/03-retrieval-wiring/03-04-SUMMARY.md` recording the demo result: the unscripted question asked, pass/fail per SC-2-SC-5, screenshots or notes, and any FDA-data-gap flagged against Phase 1. If any criterion failed, list defects for `/gsd:plan-phase --gaps`.
</output>
