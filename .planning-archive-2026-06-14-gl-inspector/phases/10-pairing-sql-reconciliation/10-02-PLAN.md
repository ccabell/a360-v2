---
phase: 10-pairing-sql-reconciliation
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - supabase/compile_sql/06-02-canonical-common-inserts.sql
  - supabase/compile_sql/06-02-conditional-compatible-inserts.sql
  - supabase/compile_sql/06-02-do-not-market-inserts.sql
  - supabase/EXECUTION_MANIFEST.json
autonomous: true
requirements: [PAIR-01, PAIR-02]

must_haves:
  truths:
    - "06-02-canonical-common-inserts.sql matches the content in REVIEW_QUEUE/pairings/ review cards with zero podcast contamination"
    - "06-02-conditional-compatible-inserts.sql has zero podcast contamination in any production field"
    - "06-02-do-not-market-inserts.sql has zero podcast contamination in any production field"
    - "All three SQL files use production-safe evidence language per EVIDENCE_MODEL.md"
    - "Sculptra pairs appear in the correct SQL file matching their tier from SCULPTRA_TIER_EVALUATION.md"
    - "EXECUTION_MANIFEST.json updated: all three 06-02 files have out_of_sync: false"
  artifacts:
    - path: "supabase/compile_sql/06-02-canonical-common-inserts.sql"
      provides: "INSERT statements for canonical + common tier pairs"
      contains: "INSERT INTO item_relationships"
    - path: "supabase/compile_sql/06-02-conditional-compatible-inserts.sql"
      provides: "INSERT statements for conditional + compatible_only tier pairs"
      contains: "INSERT INTO item_relationships"
    - path: "supabase/compile_sql/06-02-do-not-market-inserts.sql"
      provides: "INSERT statements for do_not_market tier pairs"
      contains: "INSERT INTO item_relationships"
    - path: "supabase/EXECUTION_MANIFEST.json"
      provides: "Updated manifest with out_of_sync flags cleared"
      contains: "06-02"
  key_links:
    - from: "REVIEW_QUEUE/pairings/*.md"
      to: "supabase/compile_sql/06-02-canonical-common-inserts.sql"
      via: "Review card content fields mapped to SQL INSERT values"
      pattern: "clinical_rationale.*timing_guidance.*patient_education_text"
    - from: "supabase/EXECUTION_MANIFEST.json"
      to: "supabase/compile_sql/06-02-*.sql"
      via: "Manifest tracks sync status of all SQL files"
      pattern: "out_of_sync.*false"
---

<objective>
Regenerate all three 06-02 SQL INSERT files from the finalized, clean review cards. The current SQL files contain 100+ podcast contamination instances and are completely out of sync with the review cards. This plan reads each review card, extracts its production fields, and emits clean SQL. Then updates EXECUTION_MANIFEST.json to clear the out_of_sync flags.

Purpose: PAIR-01 requires 06-02-canonical-common-inserts.sql regenerated clean. PAIR-02 requires Sculptra pairs in the correct file matching their re-evaluated tier. This is the final deliverable of v1.1 milestone.

Output: Three regenerated SQL files + updated manifest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-podcast-data-strategy-and-evidence-provenance/EVIDENCE_MODEL.md
@.planning/phases/10-pairing-sql-reconciliation/10-01-SUMMARY.md

<interfaces>
<!-- SQL file structure from existing 06-02-canonical-common-inserts.sql -->
<!-- Each INSERT follows this pattern: -->
INSERT INTO item_relationships (
  id, item_a_id, item_b_id,
  relationship_type, relationship_strength, pairing_tier,
  clinical_rationale, timing_guidance, same_session_ok,
  patient_education_text, staff_talking_points,
  is_bidirectional, is_active, source_reference
)
SELECT
  gen_random_uuid(),
  '{uuid_a}'::uuid,
  '{uuid_b}'::uuid,
  '{type}'::relationship_type,
  '{strength}'::relationship_strength,
  '{tier}',
  '{clinical_rationale}',
  '{timing_guidance}',
  {same_session_ok_boolean},
  '{patient_education_text}',
  '{staff_talking_points}',
  true,
  false,
  '{source_reference}'
WHERE NOT EXISTS (
  SELECT 1 FROM item_relationships
  WHERE (item_a_id = '{uuid_a}'::uuid AND item_b_id = '{uuid_b}'::uuid)
     OR (item_a_id = '{uuid_b}'::uuid AND item_b_id = '{uuid_a}'::uuid)
);

<!-- Product UUIDs (from existing SQL file): -->
Botox Cosmetic:          4b92be36-e84e-432c-8549-f5d85a767fdb
Juvederm Vollure XC:     7370545f-97a3-4519-a92d-3ac4f969829d
Juvederm Voluma XC:      6c8f72f0-887f-484a-a588-0bb9bd8052c9
Restylane Lyft:          f1732c7c-3f19-4f3d-9aff-543a132e5506
RHA Redensity:           (extract from existing SQL)
Sculptra Aesthetic:      (extract from existing SQL)
SKINVIVE by Juvederm:    (extract from existing SQL)
InMode Morpheus8:        (extract from existing SQL)
Daxxify:                 (extract from existing SQL)
Dysport:                 (extract from existing SQL)
Jeuveau:                 (extract from existing SQL)
Xeomin:                  (extract from existing SQL)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate all three 06-02 SQL files from clean review cards (PAIR-01)</name>
  <files>
    supabase/compile_sql/06-02-canonical-common-inserts.sql
    supabase/compile_sql/06-02-conditional-compatible-inserts.sql
    supabase/compile_sql/06-02-do-not-market-inserts.sql
  </files>
  <read_first>
    supabase/compile_sql/06-02-canonical-common-inserts.sql (extract product UUIDs and SQL structure — DO NOT copy contaminated content)
    supabase/compile_sql/06-02-conditional-compatible-inserts.sql (extract product UUIDs and SQL structure)
    supabase/compile_sql/06-02-do-not-market-inserts.sql (extract product UUIDs and SQL structure)
    .planning/phases/10-pairing-sql-reconciliation/10-01-SUMMARY.md (tier decisions from Plan 01)
    .planning/phases/10-pairing-sql-reconciliation/SCULPTRA_TIER_EVALUATION.md (final Sculptra tier verdicts)
    All 37 REVIEW_QUEUE/pairings/*.md files (source of truth for content)
  </read_first>
  <action>
    Regenerate all three SQL files by reading content from the review cards (source of truth) and emitting clean SQL.

    **Step 1: Build product UUID lookup**
    Extract all product UUIDs from the existing SQL files. Build a map: product_name -> uuid. These UUIDs are the stable identifiers — they do not change.

    **Step 2: Build tier-to-file assignment**
    From SCULPTRA_TIER_EVALUATION.md and review card Tier: headers, determine which pairs go in which file:
    - canonical + common tier -> 06-02-canonical-common-inserts.sql
    - conditional + compatible_only tier -> 06-02-conditional-compatible-inserts.sql
    - do_not_market tier -> 06-02-do-not-market-inserts.sql

    **Step 3: For each review card, extract these fields:**
    - Tier (from header)
    - Type (relationship_type — complementary, sequential, stacks_with, etc.)
    - Strength (relationship_strength — strong, moderate, etc.)
    - Same Session OK (boolean — true/false/conditional. If "conditional", use true in SQL but add the conditional language to timing_guidance)
    - Clinical Rationale (from ### Clinical Rationale section)
    - Timing Guidance (from ### Timing Guidance section)
    - Patient Education Text (from ### Patient Education Text section)
    - Staff Talking Points (from ### Staff Talking Points section)
    - source_reference: Build from the Evidence section. Use ONLY production-citable sources:
      - PubMed DOIs/PMIDs
      - FDA label references
      - "Expert consensus" (generic, no attribution)
      - DO NOT include any podcast references, internal file paths, or speaker names

    **Step 4: Emit SQL for each pair**
    Use the exact INSERT structure from the existing files (see interfaces block). Key rules:
    - `id`: gen_random_uuid()
    - `is_bidirectional`: true
    - `is_active`: false (D-06: Chris review required before publication)
    - `source_reference`: Production-safe only. Typical format: "PubMed DOI 10.xxxx/yyyy; FDA label — [Product]; Expert consensus"
    - Escape single quotes in text fields by doubling them: ' -> ''
    - WHERE NOT EXISTS dedup on both orderings of item_a_id/item_b_id

    **Step 5: File headers**
    Each file gets a header comment block:
    ```sql
    -- Phase 06 Plan 04 / Phase 10 Reconciled: {tier} tier INSERT statements
    -- Regenerated {date} from REVIEW_QUEUE/pairings/ review cards
    -- Podcast contamination removed per EVIDENCE_MODEL.md (Phase 09)
    -- All rows: is_active = false (Chris review required before publication)
    -- Enum casts: ::relationship_type, ::relationship_strength
    -- Dedup: WHERE NOT EXISTS on both orderings
    ```

    **Step 6: Final contamination scan**
    After generating each file, scan the output for contamination patterns:
    - "podcast expert", "podcast consensus", "podcast show"
    - "PHASE_6_ANSWERS", episode UUIDs, speaker names
    - Any internal file path references
    If ANY contamination found, fix before writing the file.

    **Step 7: Count verification**
    - canonical-common file: should have 5 canonical + N common pairs (N depends on Sculptra re-evaluation from Plan 01)
    - conditional-compatible file: should have the remaining conditional + compatible_only pairs
    - do-not-market file: should have the do_not_market pairs
    - Total across all three files should equal 153 (all pairs from Phase 06)

    IMPORTANT: The conditional-compatible and do-not-market SQL files also have podcast contamination (52 and 10 matches respectively). These files cover pairs that do NOT have review cards in REVIEW_QUEUE/pairings/ (only the 37 canonical/common pairs have cards). For the conditional/compatible/do-not-market pairs WITHOUT review cards:
    - Read the existing SQL to extract the content
    - Clean any contamination patterns by replacing with production-safe language:
      - "podcast expert consensus" -> "expert consensus"
      - "podcast consensus supports" -> "expert consensus supports"
      - Any "PHASE_6_ANSWERS_PODCAST_SOURCED.md: ..." in source_reference -> replace with the production-citable sources listed (PubMed DOIs, FDA labels, "expert consensus")
      - Speaker names -> remove entirely
      - Episode UUIDs -> remove entirely
    - Preserve the clean content as-is
  </action>
  <verify>
    <automated>grep -c "podcast expert\|podcast consensus\|PHASE_6_ANSWERS\|Dr\. Teri\|Tracy Mancuso\|episode [a-f0-9]\{8\}" supabase/compile_sql/06-02-canonical-common-inserts.sql supabase/compile_sql/06-02-conditional-compatible-inserts.sql supabase/compile_sql/06-02-do-not-market-inserts.sql</automated>
    All three files must show count 0 for contamination patterns. Each file must contain INSERT INTO item_relationships.
  </verify>
  <acceptance_criteria>
    - grep for contamination patterns returns 0 for all three SQL files
    - 06-02-canonical-common-inserts.sql contains INSERT statements matching the review card count for canonical + common tiers
    - All three files use the standard INSERT structure with WHERE NOT EXISTS dedup
    - source_reference fields contain only production-citable sources (PubMed, FDA, expert consensus)
    - No file contains "WARNING" or "DO NOT EXECUTE" header (those are removed — files are now reconciled)
    - Sculptra pairs appear in the file matching their tier from SCULPTRA_TIER_EVALUATION.md
  </acceptance_criteria>
  <done>All three 06-02 SQL files regenerated from clean review cards with zero podcast contamination. Content matches review cards exactly. Sculptra pairs in correct tier file.</done>
</task>

<task type="auto">
  <name>Task 2: Update EXECUTION_MANIFEST.json — clear out_of_sync flags (PAIR-01)</name>
  <files>
    supabase/EXECUTION_MANIFEST.json
  </files>
  <read_first>
    supabase/EXECUTION_MANIFEST.json
  </read_first>
  <action>
    Update the three 06-02 entries in EXECUTION_MANIFEST.json:

    1. Find entries for:
       - supabase/compile_sql/06-02-canonical-common-inserts.sql
       - supabase/compile_sql/06-02-conditional-compatible-inserts.sql
       - supabase/compile_sql/06-02-do-not-market-inserts.sql

    2. For each entry, update:
       - `out_of_sync`: false (was true — now reconciled)
       - `do_not_execute`: false (if present — files are now safe to execute)
       - Add or update: `reconciled_date`: "{today's date}"
       - Add or update: `reconciled_by`: "Phase 10 — podcast contamination removed, content synced with REVIEW_QUEUE/pairings/ review cards"

    3. Keep all other fields unchanged (status, depends_on, phase, plan, type, execution_method, etc.)

    4. Also check if 07-03-pair-timing.sql has a blocked status due to 06-02 dependencies. If so, update its status from "blocked" to "pending" since the 06-02 files are now reconciled (though still deferred for execution).

    5. Validate JSON is well-formed after edit.
  </action>
  <verify>
    <automated>node -e "const m=require('./supabase/EXECUTION_MANIFEST.json'); const files=m.files||m; const entries=(Array.isArray(files)?files:Object.values(files)).filter(e=>e.file&&e.file.includes('06-02')); entries.forEach(e=>console.log(e.file, 'out_of_sync:', e.out_of_sync)); console.log('valid JSON: true')"</automated>
    All three 06-02 entries must show out_of_sync: false.
  </verify>
  <acceptance_criteria>
    - EXECUTION_MANIFEST.json is valid JSON (node can parse it)
    - All three 06-02 entries have out_of_sync: false
    - No other manifest entries are modified except 06-02 and optionally 07-03
    - reconciled_date and reconciled_by fields present on all three 06-02 entries
  </acceptance_criteria>
  <done>EXECUTION_MANIFEST.json updated to reflect reconciled state of all three 06-02 SQL files. out_of_sync flags cleared. Phase 10 reconciliation is complete.</done>
</task>

</tasks>

<verification>
1. `grep -c "podcast expert\|podcast consensus\|PHASE_6_ANSWERS\|Dr\. Teri\|Tracy Mancuso" supabase/compile_sql/06-02-*.sql` returns 0 for all files
2. `node -e "JSON.parse(require('fs').readFileSync('supabase/EXECUTION_MANIFEST.json'))"` succeeds (valid JSON)
3. Each SQL file begins with a reconciliation header (no WARNING/DO NOT EXECUTE)
4. Total INSERT count across all three files accounts for all 153 pairs
</verification>

<success_criteria>
- All three 06-02 SQL files regenerated clean from review cards (PAIR-01)
- Sculptra pairs in correct tier files per SCULPTRA_TIER_EVALUATION.md (PAIR-02)
- EXECUTION_MANIFEST.json updated with out_of_sync: false (PAIR-01)
- Zero podcast contamination in any production field across all SQL files
</success_criteria>

<output>
After completion, create `.planning/phases/10-pairing-sql-reconciliation/10-02-SUMMARY.md`
</output>
