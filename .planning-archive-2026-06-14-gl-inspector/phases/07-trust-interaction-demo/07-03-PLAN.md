---
phase: 07-trust-interaction-demo
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified: []
autonomous: false
requirements:
  - DEMO-01

must_haves:
  truths:
    - "/ask?query=Can+Botox+and+filler+be+done+the+same+day loads without login"
    - "Query auto-submits and streams a cited answer"
    - "Key Points card appears above the answer prose"
    - "Answer contains section headers rendered as styled h3 elements"
    - "At least one table renders as HTML table element"
    - "Reference cards include Reliable badges on trusted sources"
    - "Disclaimer bar is visible at the bottom"
    - "Follow-up suggestion pills appear after answer completes"
  artifacts:
    - path: "app/ask/page.tsx"
      provides: "Public ask page with query param seeding"
      contains: "initialQuery"
    - path: "components/ask/ask-experience.tsx"
      provides: "Auto-submit and full rendering pipeline"
      contains: "autoSubmitInitial"
  key_links:
    - from: "app/ask/page.tsx"
      to: "components/ask/ask-experience.tsx"
      via: "initialQuery and autoSubmitInitial props"
      pattern: "autoSubmitInitial.*Boolean"
    - from: "components/ask/ask-experience.tsx"
      to: "app/api/ask/route.ts"
      via: "streamFrom fetch to endpoint"
      pattern: "streamFrom.*endpoint"
---

<objective>
End-to-end demo acceptance test: verify the complete Boulevard demo scenario works from URL load to follow-up pills.

Purpose: This is the final gate before the June 22 demo. The demo URL `/ask?query=Can+Botox+and+filler+be+done+the+same+day` must load, auto-submit, stream a fully-featured answer, and look polished. Any issues discovered here get fixed surgically.

Output: Verified working demo flow, or targeted fixes applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-trust-interaction-demo/07-01-SUMMARY.md
@.planning/phases/07-trust-interaction-demo/07-02-SUMMARY.md

@app/ask/page.tsx
@components/ask/ask-experience.tsx
@app/api/ask/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-flight checks for demo scenario</name>
  <files></files>
  <read_first>app/ask/page.tsx, components/ask/ask-experience.tsx, app/api/ask/route.ts, components/grounding/key-points-card.tsx, components/grounding/grounded-answer.tsx, components/grounding/answer-message.tsx</read_first>
  <action>
Run automated verification of the demo pipeline by reading the code path end-to-end:

1. **Query seeding**: Confirm app/ask/page.tsx reads searchParams.query, passes it as initialQuery and autoSubmitInitial={Boolean(query)} to AskExperience.

2. **Auto-submit**: Confirm ask-experience.tsx useEffect calls send(initialQuery) when autoSubmitInitial is true.

3. **SSE streaming**: Confirm streamFrom makes a POST to endpoint with query in body, parses SSE frames.

4. **Key Points**: Confirm route.ts extractKeyPoints parses KEY_POINTS sentinel, and ask-experience.tsx renders KeyPointsCard when m.keyPoints exists.

5. **Section headers**: Confirm answer-message.tsx detects bold headings and renders h3 elements.

6. **Tables**: Confirm answer-message.tsx detects markdown tables and renders MarkdownTable component.

7. **Reliable badges**: Confirm citation cards show reliable badges for trusted-tier sources.

8. **Disclaimer**: Confirmed in 07-01.

9. **Follow-up pills**: Confirm route.ts extractFollowUps parses FOLLOW_UPS sentinel, done event sends followUps, and ask-experience.tsx renders follow-up buttons.

If any code path is broken (e.g., missing import, wrong prop name, broken parser), apply a surgical fix. Do NOT refactor working code.

Run `npx next build` to confirm no compile errors.
  </action>
  <verify>
    <automated>cd C:/projects/a360-v2-wse && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- Build succeeds with exit code 0
- grep -q "autoSubmitInitial" app/ask/page.tsx returns 0
- grep -q "KeyPointsCard" components/ask/ask-experience.tsx returns 0
- grep -q "MarkdownTable" components/grounding/answer-message.tsx returns 0
- grep -q "followUps" app/api/ask/route.ts returns 0
- grep -q "Reliable" components/grounding/citation-hover-card.tsx returns 0
  </acceptance_criteria>
  <done>All code paths for the demo scenario verified. Build passes. No broken wiring found (or surgical fixes applied).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live demo scenario verification</name>
  <files></files>
  <action>
Human verifies the live demo scenario end-to-end. Claude starts the dev server if not running, then pauses for human verification.

What was built: Complete demo pipeline from Phase 4-7 — Key Points card, structured sections, tables, source display with reliable badges, follow-up pills with A360 styling, disclaimer, suggestion chips, and the hardened system prompt.

Steps for human:
1. Start dev server: `npm run dev`
2. Open browser to: `http://localhost:3000/ask?query=Can+Botox+and+filler+be+done+the+same+day`
3. Verify the following (check each):
   - Page loads without login prompt
   - Query auto-submits (you see "Searching..." status)
   - Source count bar appears ("N sources found")
   - Answer streams in with visible text
   - Key Points card appears above the answer prose with bulleted takeaways
   - Section headers are visually distinct (h3 with border-left)
   - At least one table renders as an HTML table (not raw markdown pipes)
   - Reference cards below answer show "Reliable" green badge on FDA/manufacturer sources
   - Inline citation badges show hover card popover on mouse hover
   - Disclaimer bar is visible at bottom: "not medical advice"
   - Follow-up suggestion pills appear after answer completes, styled with cool-tone accent colors
   - Clicking a follow-up pill sends a new question and streams a new answer
4. Spot-check: answer does NOT contain uncited safety claims (dosing, contraindications mentioned without [src_N] markers)

Resume signal: Type "approved" if all checks pass, or describe specific issues to fix.
  </action>
  <verify>Human approval received</verify>
  <done>Human confirms all 12 demo checklist items pass. DEMO-01 accepted.</done>
</task>

</tasks>

<verification>
- Build passes
- All code paths verified via grep
- Human confirms live demo scenario works end-to-end
</verification>

<success_criteria>
DEMO-01: The URL `/ask?query=Can+Botox+and+filler+be+done+the+same+day` loads without login, auto-submits, streams a cited answer with Key Points card, section headers, at least one table, Reliable-badged references, visible disclaimer, and follow-up pills.
</success_criteria>

<output>
After completion, create `.planning/phases/07-trust-interaction-demo/07-03-SUMMARY.md`
</output>
