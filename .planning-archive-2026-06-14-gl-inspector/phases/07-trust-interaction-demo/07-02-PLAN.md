---
phase: 07-trust-interaction-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ask/ask-experience.tsx
  - components/ask/ask-suggestion-chips.tsx
autonomous: true
requirements:
  - INTR-01
  - INTR-02

must_haves:
  truths:
    - "Follow-up suggestion pills use A360 brand styling with bg-accent or bg-muted tones instead of raw bg-card"
    - "Follow-up pills are clickable and seed a new question when clicked"
    - "Suggestion chips on initial state render correctly with verb grouping"
    - "Suggestion chips trigger a query when clicked"
  artifacts:
    - path: "components/ask/ask-experience.tsx"
      provides: "Follow-up pills with brand styling"
      contains: "bg-accent"
    - path: "components/ask/ask-suggestion-chips.tsx"
      provides: "Verb-grouped suggestion chips"
      contains: "onSelect"
  key_links:
    - from: "components/ask/ask-experience.tsx"
      to: "send function"
      via: "follow-up pill onClick calls send(fu)"
      pattern: "onClick.*send"
    - from: "components/ask/ask-suggestion-chips.tsx"
      to: "AskExperience"
      via: "onSelect prop wired to send"
      pattern: "onSelect"
---

<objective>
Polish follow-up suggestion pills to A360 brand styling and verify suggestion chips work correctly.

Purpose: Follow-up pills currently use `rounded-full border border-border bg-card` which looks generic. They should use A360 brand tones (bg-accent/bg-muted) for a polished demo appearance. Suggestion chips are believed to work — verify and leave unchanged if so.

Output: Brand-styled follow-up pills, verified suggestion chips.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@components/ask/ask-experience.tsx
@components/ask/ask-suggestion-chips.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle follow-up suggestion pills to A360 brand (INTR-01)</name>
  <files>components/ask/ask-experience.tsx</files>
  <read_first>components/ask/ask-experience.tsx</read_first>
  <action>
In components/ask/ask-experience.tsx, find the follow-up suggestion pills section (around line 446-457). The current className on the follow-up buttons is:

`"rounded-full border border-border bg-card px-3 py-1.5 text-xs text-foreground transition-colors hover:border-primary/50 hover:bg-muted disabled:opacity-50"`

Replace with A360 brand styling:

`"rounded-full border border-primary/20 bg-accent px-3 py-1.5 text-xs text-accent-foreground transition-colors hover:bg-accent/80 hover:border-primary/40 disabled:opacity-50"`

This uses bg-accent (A360 cool-tone from the theme) with primary border hints for brand consistency. The hover state darkens slightly.

Do NOT change the onClick handler, the disabled logic, or any structural JSX — only the className string.
  </action>
  <verify>
    <automated>cd C:/projects/a360-v2-wse && grep -c "bg-accent" components/ask/ask-experience.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep -q "bg-accent" components/ask/ask-experience.tsx returns 0
- grep -q "border-primary/20" components/ask/ask-experience.tsx returns 0
- grep -qv "bg-card" components/ask/ask-experience.tsx (bg-card removed from follow-up pills — note: may still exist elsewhere in file, so check the follow-up section specifically)
  </acceptance_criteria>
  <done>Follow-up pills use bg-accent with primary border hints. No functional changes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify suggestion chips functionality (INTR-02)</name>
  <files>components/ask/ask-suggestion-chips.tsx</files>
  <read_first>components/ask/ask-suggestion-chips.tsx, lib/config/ask-suggestions.ts</read_first>
  <action>
Verify INTR-02 by reading the suggestion chips implementation:

1. Read components/ask/ask-suggestion-chips.tsx — confirm it accepts VerbGroup[] and onSelect callback, renders verb buttons that toggle item expansion, and item buttons call onSelect(item.query).

2. Read lib/config/ask-suggestions.ts — confirm ASK_SUGGESTIONS array exists with verb groups and query items.

3. Confirm AskExperience passes suggestions={ASK_SUGGESTIONS} and onSelect={send} (already visible in ask-experience.tsx around line 335).

If everything works as expected, make NO changes. If any wiring is broken (e.g., onSelect not connected to send, or ASK_SUGGESTIONS missing), fix the specific broken connection.

This task is verification-only unless a defect is found. Build must pass.
  </action>
  <verify>
    <automated>cd C:/projects/a360-v2-wse && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep -q "onSelect" components/ask/ask-suggestion-chips.tsx returns 0
- grep -q "ASK_SUGGESTIONS" app/ask/page.tsx returns 0
- grep -q "suggestions=" components/ask/ask-experience.tsx returns 0 (prop wired)
  </acceptance_criteria>
  <done>Suggestion chips render on initial state with verb grouping, expand on verb click, and fire onSelect to send the query. No changes needed (or targeted fix applied if defect found).</done>
</task>

</tasks>

<verification>
- Build succeeds: `npx next build` exits 0
- Follow-up pills use bg-accent class
- Suggestion chips onSelect wired through to send
</verification>

<success_criteria>
INTR-01: Follow-up pills styled with bg-accent and cool-tone A360 palette.
INTR-02: Suggestion chips functional — verb grouping, expansion, query submission.
</success_criteria>

<output>
After completion, create `.planning/phases/07-trust-interaction-demo/07-02-SUMMARY.md`
</output>
