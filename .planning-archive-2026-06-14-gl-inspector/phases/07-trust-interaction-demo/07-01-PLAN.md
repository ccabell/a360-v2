---
phase: 07-trust-interaction-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ask/page.tsx
  - app/embed/ask/page.tsx
  - app/api/ask/route.ts
autonomous: true
requirements:
  - TRST-01
  - TRST-02
  - TRST-03

must_haves:
  truths:
    - "Disclaimer bar is visible on /ask page load without blocking content"
    - "Disclaimer bar is visible on /embed/ask page load without blocking content"
    - "Clicking an inline citation badge opens the hover card popover"
    - "System prompt instructs LLM to never make uncited claims about dosing, contraindications, safety, pregnancy, complications, off-label use, or adverse events"
  artifacts:
    - path: "app/ask/page.tsx"
      provides: "Persistent disclaimer bar"
      contains: "not medical advice"
    - path: "app/embed/ask/page.tsx"
      provides: "Embed disclaimer bar"
      contains: "not medical advice"
    - path: "app/api/ask/route.ts"
      provides: "System prompt with safety guardrails"
      contains: "contraindications"
    - path: "components/grounding/citation-hover-card.tsx"
      provides: "Hover card popover on citation badges"
      contains: "CitationHoverCard"
  key_links:
    - from: "components/grounding/answer-message.tsx"
      to: "components/grounding/citation-hover-card.tsx"
      via: "CitationHoverCard wrapping InlineAuthorityBadge"
      pattern: "CitationHoverCard"
---

<objective>
Audit and harden the three trust surfaces: disclaimer visibility, citation click behavior, and uncited-claim guardrails.

Purpose: TRST-01, TRST-02, TRST-03 are believed to already work based on prior phases. This plan verifies each, and applies the one known fix: adding explicit safety-topic guardrail language to the SYSTEM prompt so the LLM never makes uncited claims about clinical safety topics.

Output: Verified disclaimer on both surfaces, working citation hover cards, and a hardened system prompt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/ask/page.tsx
@app/embed/ask/page.tsx
@app/api/ask/route.ts
@components/grounding/citation-hover-card.tsx
@components/grounding/answer-message.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify disclaimer and citation hover card — fix if broken</name>
  <files>app/ask/page.tsx, app/embed/ask/page.tsx</files>
  <read_first>app/ask/page.tsx, app/embed/ask/page.tsx, components/grounding/citation-hover-card.tsx, components/grounding/answer-message.tsx</read_first>
  <action>
Verify TRST-01 (disclaimer) and TRST-02 (citation click):

1. **TRST-01 — Disclaimer visibility**: Read app/ask/page.tsx and confirm the disclaimer div exists at bottom with `border-t border-border bg-background` and text "not medical advice". Read app/embed/ask/page.tsx and confirm same pattern. Both must be outside the scrollable content area so they remain visible. If either is missing or inside the scrollable area, fix positioning.

2. **TRST-02 — Citation click behavior**: Read components/grounding/citation-hover-card.tsx and confirm it renders a popover on hover/focus. Read components/grounding/answer-message.tsx to confirm InlineAuthorityBadge is wrapped by CitationHoverCard. If the wiring is broken or missing, fix it.

Both features are expected to already work from prior phases. This task is verification with surgical fixes only if something is broken. Do NOT restyle or refactor working code.
  </action>
  <verify>
    <automated>cd C:/projects/a360-v2-wse && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep -q "not medical advice" app/ask/page.tsx returns 0
- grep -q "not medical advice" app/embed/ask/page.tsx returns 0
- grep -q "CitationHoverCard" components/grounding/answer-message.tsx returns 0
  </acceptance_criteria>
  <done>Disclaimer bar present on both /ask and /embed/ask surfaces outside scrollable content. Citation hover card wired to inline badges.</done>
</task>

<task type="auto">
  <name>Task 2: Harden system prompt with explicit uncited-claim guardrails (TRST-03)</name>
  <files>app/api/ask/route.ts</files>
  <read_first>app/api/ask/route.ts</read_first>
  <action>
In app/api/ask/route.ts, add explicit safety-topic guardrail language to the SYSTEM constant. After the existing line "If the sources do not support an answer, say so.", add:

"NEVER make claims about dosing, contraindications, safety warnings, drug interactions, sequencing restrictions, pregnancy/lactation, complications, adverse events, or off-label use without citing a specific [src_N] source. If sources lack safety data for the question, explicitly state that the sources do not cover that safety topic rather than speculating."

This is an append to the existing SYSTEM string — do not restructure or reword the existing prompt content. The addition goes before the KEY_POINTS instruction line.
  </action>
  <verify>
    <automated>cd C:/projects/a360-v2-wse && grep -c "contraindications" app/api/ask/route.ts</automated>
  </verify>
  <acceptance_criteria>
- grep -q "contraindications" app/api/ask/route.ts returns 0
- grep -q "adverse events" app/api/ask/route.ts returns 0
- grep -q "off-label" app/api/ask/route.ts returns 0
- grep -q "pregnancy" app/api/ask/route.ts returns 0
  </acceptance_criteria>
  <done>System prompt explicitly forbids uncited claims on all safety topics listed in TRST-03. Build passes.</done>
</task>

</tasks>

<verification>
- Build succeeds: `npx next build` exits 0
- Disclaimer text present in both page files
- CitationHoverCard wired in answer-message.tsx
- System prompt contains safety guardrail keywords
</verification>

<success_criteria>
TRST-01: Disclaimer visible on /ask and /embed/ask without blocking content.
TRST-02: Citation hover card popover opens on inline badge hover.
TRST-03: System prompt explicitly requires citations for all safety-related claims.
</success_criteria>

<output>
After completion, create `.planning/phases/07-trust-interaction-demo/07-01-SUMMARY.md`
</output>
