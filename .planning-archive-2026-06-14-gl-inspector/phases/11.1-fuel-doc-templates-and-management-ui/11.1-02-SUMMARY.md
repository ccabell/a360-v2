---
phase: 11.1-fuel-doc-templates-and-management-ui
plan: 02
subsystem: api
tags: [nextjs, api-routes, supabase, sql, migration, sidebar]

# Dependency graph
requires:
  - phase: 11.1-01
    provides: "lib/api/fuel-docs.ts functions and lib/types/fuel-docs.ts types"
provides:
  - "GET /api/fuel-docs — list fuel docs with type/status/search filtering"
  - "POST /api/fuel-docs — create fuel doc returning 201"
  - "GET /api/fuel-docs/{id} — single fuel doc with optional COALESCE resolution via practice_id"
  - "PATCH /api/fuel-docs/{id} — update fuel doc"
  - "DELETE /api/fuel-docs/{id} — delete fuel doc returning 204"
  - "Fuel Library sidebar entry with BookOpen icon, scope internal"
  - "supabase/migrations/11.1_fuel_doc_schema.sql — idempotent DDL for gl + pl fuel doc tables"
affects: [11.1-03, 11.1-04, phase-12]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Next.js 16 async params pattern: params: Promise<{ id: string }> + await params"
    - "COALESCE resolution wired end-to-end: ?practice_id query param triggers getResolvedFuelDoc"
    - "scope: internal pattern for sidebar items hidden in demo mode (APP_MODE=demo)"

key-files:
  created:
    - app/api/fuel-docs/route.ts
    - app/api/fuel-docs/[id]/route.ts
    - supabase/migrations/11.1_fuel_doc_schema.sql
  modified:
    - components/layout/sidebar.tsx

key-decisions:
  - "COALESCE resolution wired at HTTP layer — GET /api/fuel-docs/{id}?practice_id=X returns merged gl+pl content without exposing internal function to consumers"
  - "Migration SQL is reference artifact only — not auto-executed; idempotent with IF NOT EXISTS guards for safe future application"
  - "Fuel Library sidebar entry uses scope: internal, consistent with Agent Manager pattern — hidden in acquirer demo build"

patterns-established:
  - "API route pattern: force-dynamic + typed query params cast from searchParams + 502 on list errors + 201 on create + 204 on delete"
  - "Dynamic route pattern: params: Promise<{ id: string }> + await params (Next.js 16)"

requirements-completed: [FUEL-UI-02, FUEL-INT-01]

# Metrics
duration: 15min
completed: 2026-06-14
---

# Phase 11.1 Plan 02: API Routes, Sidebar, and Schema Migration Summary

**Five-verb CRUD API at /api/fuel-docs with Next.js 16 async params, Fuel Library sidebar entry, and idempotent schema migration SQL for gl + pl fuel doc tables**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-06-14T17:00:00Z
- **Completed:** 2026-06-14T17:15:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Created GET + POST routes at /api/fuel-docs with type/status/search query params and matching agents API error handling
- Created GET + PATCH + DELETE routes at /api/fuel-docs/{id} with COALESCE resolution when practice_id provided
- Added Fuel Library sidebar entry with BookOpen icon after Agent Manager, before Agent Tester, scope internal
- Created idempotent migration SQL defining pl_agent_fuel_documents table and structured columns for gl_agent_fuel_documents

## Task Commits

Each task was committed atomically:

1. **Task 1: Create API routes for fuel doc CRUD** - `15c3afd` (feat)
2. **Task 2: Add sidebar entry and create migration SQL** - `5e932c7` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `app/api/fuel-docs/route.ts` - GET list (type/status/search filters) + POST create (201)
- `app/api/fuel-docs/[id]/route.ts` - GET single/resolved + PATCH update + DELETE (204)
- `components/layout/sidebar.tsx` - Added BookOpen import + Fuel Library menu entry
- `supabase/migrations/11.1_fuel_doc_schema.sql` - Idempotent DDL for gl columns + pl table + indexes

## Decisions Made
- COALESCE resolution wired at HTTP layer: GET /api/fuel-docs/{id}?practice_id=X transparently calls getResolvedFuelDoc, so UI pages (Plans 03-04) need no knowledge of the two-layer model
- Migration SQL is a reference artifact only — not auto-executed via Supabase CLI; guards allow safe application at any time
- Fuel Library sidebar entry scoped internal (hidden in demo/acquirer mode) consistent with Agent Manager

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None — no external service configuration required. Migration SQL is a reference artifact; apply via Supabase dashboard when ready.

## Next Phase Readiness
- All HTTP endpoints for fuel doc CRUD are live and TypeScript-clean
- UI pages (Plans 03-04) can call /api/fuel-docs and /api/fuel-docs/{id} directly
- Fuel Library appears in sidebar under internal scope — navigation is wired before pages exist
- Migration SQL ready to apply when Supabase CLI is available

---
*Phase: 11.1-fuel-doc-templates-and-management-ui*
*Completed: 2026-06-14*

## Self-Check: PASSED

- FOUND: app/api/fuel-docs/route.ts
- FOUND: app/api/fuel-docs/[id]/route.ts
- FOUND: supabase/migrations/11.1_fuel_doc_schema.sql
- FOUND: .planning/phases/11.1-fuel-doc-templates-and-management-ui/11.1-02-SUMMARY.md
- FOUND commit 15c3afd: feat(11.1-02): add fuel doc CRUD API routes at /api/fuel-docs
- FOUND commit 5e932c7: feat(11.1-02): add Fuel Library sidebar entry and schema migration SQL
