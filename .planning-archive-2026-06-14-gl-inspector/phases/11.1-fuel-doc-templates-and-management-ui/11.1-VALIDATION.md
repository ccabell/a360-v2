---
phase: 11.1
slug: fuel-doc-templates-and-management-ui
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-14
---

# Phase 11.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Primary automated check** | `npx tsc --noEmit` (TypeScript compilation) |
| **Behavioral check** | `npx tsx` one-liner for serializer (Plan 01 Task 3) |
| **E2E framework** | Playwright (`@playwright/test` 1.60.0) — available but not primary for this phase |
| **Quick run command** | `npx tsc --noEmit 2>&1 \| grep "error TS" \| head -20` |
| **Estimated runtime** | ~15 seconds |

**Note:** This phase is a UI + data layer build. TypeScript compilation (`tsc --noEmit`) is the primary automated verification layer — it catches type errors, missing imports, and interface mismatches across all files. Playwright is installed in the project and available for future E2E smoke tests, but `tsc` provides the automated feedback loop for this phase. No vitest configuration exists in the project; the checker flagged vitest references as inconsistent with the actual stack.

---

## Sampling Rate

- **After every task commit:** Run `npx tsc --noEmit 2>&1 | grep "error TS" | head -20`
- **After every plan wave:** Run `npx tsc --noEmit`
- **Before `/gsd:verify-work`:** Full tsc must pass with zero errors
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Status |
|---------|------|------|-------------|-----------|-------------------|--------|
| 01-T1 | 01 | 1 | Type definitions | tsc compile | `npx tsc --noEmit 2>&1 \| grep -i "fuel-docs" \| head -20` | ⬜ pending |
| 01-T2 | 01 | 1 | Data access layer | tsc compile | `npx tsc --noEmit 2>&1 \| grep -i "fuel-docs" \| head -20` | ⬜ pending |
| 01-T3 | 01 | 1 | MD serializer | tsc + behavioral | `npx tsc --noEmit` + `npx tsx` serializer check | ⬜ pending |
| 02-T1 | 02 | 2 | API routes | tsc compile | `npx tsc --noEmit 2>&1 \| grep -i "fuel-docs" \| head -10` | ⬜ pending |
| 02-T2 | 02 | 2 | Sidebar + migration | grep + file exists | `grep "Fuel Library" components/layout/sidebar.tsx` | ⬜ pending |
| 03-T1 | 03 | 3 | Badge + filter components | tsc compile | `npx tsc --noEmit 2>&1 \| grep "error TS" \| head -20` | ⬜ pending |
| 03-T2 | 03 | 3 | List page + create dialog | tsc compile | `npx tsc --noEmit 2>&1 \| grep "error TS" \| head -20` | ⬜ pending |
| 04-T1 | 04 | 4 | Detail page + tabs | tsc compile | `npx tsc --noEmit 2>&1 \| grep "error TS" \| head -20` | ⬜ pending |
| 04-T2 | 04 | 4 | Visual verification | manual | Human walkthrough (15 steps) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Wave 0 is satisfied by `tsc --noEmit` which is already configured in the project (tsconfig.json exists). No additional test stubs are needed because:

1. TypeScript compilation catches type errors, missing imports, and interface mismatches — the primary failure modes for a data layer + UI phase.
2. The serializer behavioral check (Plan 01 Task 3) uses `npx tsx` to execute the function at runtime.
3. Plan 04 includes a human verification checkpoint for visual/functional correctness.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| UI CRUD operations | FUEL-UI-01 | Browser interaction required | Navigate to /dashboard/fuel-library, create/edit/delete docs |
| Practice override display | FUEL-TPL-02 | Visual + data flow | SOPs tab shows practice-level message |
| Template MD readability | FUEL-SER-01 | Content quality check | Review serialized markdown for coherent structure |
| Sidebar placement | FUEL-INT-01 | Visual positioning | Fuel Library between Agent Manager and Agent Tester |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify (tsc compile checks)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covered by tsc --noEmit (no MISSING references)
- [x] No watch-mode flags
- [x] Feedback latency < 30s (~15s for tsc)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validated
