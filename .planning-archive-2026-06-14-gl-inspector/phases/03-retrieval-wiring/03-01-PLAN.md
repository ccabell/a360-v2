---
phase: 03-retrieval-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.local.example
autonomous: false
requirements: []
must_haves:
  truths:
    - "AI_GATEWAY_API_KEY is declared in .env.local.example and confirmed present (or flagged absent) in .env.local"
    - "The Botox offering_id and Neurotoxins category_id are verified against the live Agent Manager Supabase"
    - "evidence_links and agent_reference_docs rows exist for Botox so the Wave 1 route returns non-empty sources"
  artifacts:
    - path: ".env.local.example"
      provides: "AI_GATEWAY_API_KEY env var declaration"
      contains: "AI_GATEWAY_API_KEY"
  key_links:
    - from: ".env.local.example"
      to: "@ai-sdk/gateway runtime"
      via: "AI_GATEWAY_API_KEY env var name"
      pattern: "AI_GATEWAY_API_KEY"
---

<objective>
Wave 0 prep for the retrieval route. Add the one new env var (`AI_GATEWAY_API_KEY`) the Vercel AI Gateway needs, and verify the demo data assumptions the route hard-codes: the Botox offering_id, the Neurotoxins category_id, and that real `evidence_links` + `agent_reference_docs` rows exist for Botox.

Purpose: De-risk Wave 1. If the gateway key is missing or the hard-coded IDs are wrong, the route silently returns zero sources and the demo fails. Catch that here, before any code is written against those assumptions.
Output: Updated `.env.local.example`, plus a verification checkpoint that confirms (or corrects) the IDs and row counts the route in Plan 02 depends on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-retrieval-wiring/03-RESEARCH.md
@.planning/phases/03-retrieval-wiring/03-CONTEXT.md
@.env.local.example
@lib/supabase.ts

<interfaces>
<!-- agentSupabase client (Agent Manager project aejskvmpembryunnbgrk). Already configured. -->
From lib/supabase.ts:
```typescript
export const agentSupabase: SupabaseClient; // reads NEXT_PUBLIC_AGENT_SUPABASE_URL + AGENT_SUPABASE_SERVICE_KEY
```

Hard-coded IDs the Plan 02 route will use (confirmed across Phase 2 artifacts — verify here):
- Botox Cosmetic offering_id: 4b92be36-e84e-432c-8549-f5d85a767fdb
- Neurotoxins category_id: 57b7c5a8-0799-42b0-9111-8441f18a82db
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI_GATEWAY_API_KEY to .env.local.example</name>
  <files>.env.local.example</files>
  <read_first>
    - .env.local.example (the file being modified)
    - .planning/phases/03-retrieval-wiring/03-RESEARCH.md (Pitfall 1 + Environment Availability section — explains why this key is required)
  </read_first>
  <action>
    Edit `.env.local.example`. Under the existing `# --- LLM Providers (for playground / agent testing) ---` block (currently holds `ANTHROPIC_API_KEY=` and `OPENAI_API_KEY=`), add a new line:

    ```
    # --- Vercel AI Gateway (Research/Evidence tab generation) --------------------
    # Required by @ai-sdk/gateway for the /api/research/chat route. Without it,
    # streamText throws GatewayAuthenticationError at runtime.
    AI_GATEWAY_API_KEY=
    ```

    Do NOT add a real key value. This is the example file — leave the value blank. Do not touch any other env var. Do not commit a real `.env.local`.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.env.local.example','utf8'); if(!t.includes('AI_GATEWAY_API_KEY')) process.exit(1); console.log('AI_GATEWAY_API_KEY present in example')"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.local.example` contains the literal string `AI_GATEWAY_API_KEY`
    - The value after `AI_GATEWAY_API_KEY=` is empty (no real secret committed)
    - No other line in `.env.local.example` is changed (diff touches only the new block)
  </acceptance_criteria>
  <done>`.env.local.example` declares `AI_GATEWAY_API_KEY=` with an explanatory comment; no secret value committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify gateway key + Botox data assumptions</name>
  <files>(verification only — no files modified)</files>
  <what-built>
    Wave 0 verification. Claude runs the checks below; the human confirms the gateway key is provisioned and the hard-coded IDs + data exist. This gates Plan 02 — the route is written against these exact IDs and row counts.
  </what-built>
  <read_first>
    - .planning/phases/03-retrieval-wiring/03-RESEARCH.md (Wave 0 Gaps checklist + Open Questions 2 and 6)
    - lib/supabase.ts (agentSupabase client)
  </read_first>
  <action>
    Claude runs these checks first (automate everything possible), then reports results to the human:

    1. Gateway package + key presence:
    ```bash
    node -e "console.log('gateway resolvable:', !!require('@ai-sdk/gateway'))"
    node -e "require('dotenv').config({path:'.env.local'}); console.log('AI_GATEWAY_API_KEY present:', !!process.env.AI_GATEWAY_API_KEY)"
    ```
    (If `dotenv` is not installed, read `.env.local` directly and grep for a non-empty `AI_GATEWAY_API_KEY=` line.)

    2. Verify Botox offering_id + data, using the agentSupabase client. Write a throwaway node script (or use `npx tsx`) that queries:
    - `products` WHERE name ILIKE '%botox%' — assert id `4b92be36-e84e-432c-8549-f5d85a767fdb` is in the result.
    - `evidence_links` filtered to that offering_id, grouped by `source` — record counts per source (expect at least one `pubmed` and one `fda_label`/`fda` row for the demo links to render).
    - `agent_reference_docs` filtered to `offering_id = <botox>` OR `category_id = 57b7c5a8-0799-42b0-9111-8441f18a82db`, with `lens IN ('clinical','deep_product')` and `status IN ('draft','active')` — record count (D-07 fallback: if zero, the route relies on evidence_links only; note this for Plan 02).

    Report a short table: { gateway key present: y/n, botox id confirmed: y/n, evidence_links source counts, dossier doc count }.
  </action>
  <how-to-verify>
    1. Confirm `gateway resolvable: true` printed.
    2. Confirm `AI_GATEWAY_API_KEY present: true`. If false, the human must provision the key in `.env.local` before Plan 02 generation will work (the DB/UI parts still build; only the LLM call blocks).
    3. Confirm the Botox offering_id `4b92be36-e84e-432c-8549-f5d85a767fdb` appears in the products query. If a DIFFERENT id comes back, record the correct id — Plan 02 must use the verified value.
    4. Confirm `evidence_links` for Botox has >= 1 row with `source` in (pubmed) AND >= 1 row with `source` in (fda_label, fda). If FDA rows are missing, note that SC-4 depends on Phase 1 FDA backfill having run.
    5. Reply "approved" with the verified IDs + counts, or describe corrections needed.
  </how-to-verify>
  <acceptance_criteria>
    - Console output shows `gateway resolvable: true`
    - Console output shows `AI_GATEWAY_API_KEY present:` (true required for generation; false is recorded as a known blocker)
    - The Botox offering_id is confirmed or corrected and written into the plan summary
    - evidence_links source counts for Botox are recorded (pubmed + fda presence noted)
    - agent_reference_docs doc count for Botox/Neurotoxins is recorded (D-07 fallback flag set if zero)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the verified offering_id, category_id, evidence_links source counts, and dossier doc count — or describe corrections.</resume-signal>
</task>

</tasks>

<verification>
- `.env.local.example` contains `AI_GATEWAY_API_KEY`.
- Gateway package resolves at runtime.
- Botox offering_id verified against live Agent Manager Supabase.
- evidence_links (pubmed + fda) and agent_reference_docs row presence recorded for Plan 02.
</verification>

<success_criteria>
- New env var declared (no secret committed).
- The IDs and data counts Plan 02 hard-codes are confirmed against the live DB, or corrected values are captured in the summary.
</success_criteria>

<output>
After completion, create `.planning/phases/03-retrieval-wiring/03-01-SUMMARY.md` recording: AI_GATEWAY_API_KEY status, the verified Botox offering_id, Neurotoxins category_id, evidence_links source counts, and dossier doc count. Plan 02 reads these values.
</output>
