---
phase: 11-source-framework-and-v1.1-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [CFRW-01]

must_haves:
  truths:
    - "PAIR-01 is verified complete — 06-02-canonical-common-inserts.sql regenerated from clean review cards"
    - "EVID-03 browser test completed or documented as blocked with specific blocker"
    - "TIMING_REVIEW.md decisions are saved and closed"
    - "SQL manifest execution status is documented — pending files either executed or explicitly deferred with reason"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated PAIR-01 and EVID-03 status"
      contains: "PAIR-01"
    - path: ".planning/STATE.md"
      provides: "Updated v1.1 carry-forward status"
      contains: "CFRW-01"
  key_links: []
---

<objective>
Close out all v1.1 carry-forward items so v1.2 starts on a clean foundation.

Purpose: Four items carried forward from v1.1 — PAIR-01 (SQL regeneration verification), EVID-03 (browser test), TIMING_REVIEW.md closure, and SQL manifest execution. All require Chris action.
Output: Updated REQUIREMENTS.md and STATE.md reflecting closed items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@supabase/EXECUTION_MANIFEST.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify PAIR-01 completion and document status</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - supabase/compile_sql/06-02-canonical-common-inserts.sql
    - supabase/EXECUTION_MANIFEST.json
    - .planning/phases/10-pairing-sql-reconciliation/10-02-SUMMARY.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
Verify PAIR-01 is complete by confirming:
1. Read 06-02-canonical-common-inserts.sql and grep for podcast contamination patterns: `podcast`, `Business of`, `episode`, speaker names (`Dr. Teri Fisher`, `Tracy Mancuso`). Expected: 0 matches.
2. Confirm the file has no `WARNING` or `DO NOT EXECUTE` header.
3. Confirm EXECUTION_MANIFEST.json entry for 06-02-canonical-common-inserts.sql shows `out_of_sync: false` and `do_not_execute: false`.
4. Count INSERT statements in the file. Expected: >= 31.
5. Update REQUIREMENTS.md: change `- [ ] **PAIR-01**:` to `- [x] **PAIR-01**:` on the line that reads `- [ ] **PAIR-01**: 06-02-canonical-common-inserts.sql regenerated from Chris's reviewed/approved pairing cards with no raw podcast references`.
6. In the Traceability table, change PAIR-01 status from `Pending` to `Complete`.
  </action>
  <verify>
    <automated>cd C:/projects/a360-v2 && grep -c "podcast\|Business of\|episode\|Dr. Teri Fisher\|Tracy Mancuso" supabase/compile_sql/06-02-canonical-common-inserts.sql; grep "PAIR-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - grep for podcast contamination in 06-02-canonical-common-inserts.sql returns 0
    - .planning/REQUIREMENTS.md contains `[x] **PAIR-01**`
    - .planning/REQUIREMENTS.md Traceability table contains `PAIR-01 | Phase 10 | Complete`
  </acceptance_criteria>
  <done>PAIR-01 verified complete and marked in REQUIREMENTS.md</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Chris actions — EVID-03 browser test, TIMING_REVIEW.md, SQL execution</name>
  <files>(no files — human action checkpoint)</files>
  <action>
Three v1.1 carry-forward items require Chris to act. Claude cannot perform these. Present the verification steps below and wait for Chris's response.

Item 1 — EVID-03 (Live UI browser test): Chris opens the A360 demo app, navigates to Research/Evidence tab for any product, types an unscripted clinical question, and verifies clickable PubMed + FDA links render with citation chips mapping to source panel.

Item 2 — TIMING_REVIEW.md closure: Chris reviews .planning/phases/07-timing-rules/TIMING_REVIEW.md (if it exists), approves or modifies safety-critical timing decisions.

Item 3 — SQL manifest execution: 51 SQL files pending in EXECUTION_MANIFEST.json. Chris executes them in dependency order (order 10-62) via Supabase Dashboard SQL editor.
  </action>
  <how-to-verify>

**Item 1: EVID-03 — Live UI browser test**
1. Open the A360 demo app in browser
2. Navigate to Research/Evidence tab for any product (e.g., Botox)
3. Type an unscripted clinical question
4. Verify: response renders with clickable PubMed links (pubmed.ncbi.nlm.nih.gov URLs) and at least one FDA link (accessdata.fda.gov URL)
5. Verify: citation chips [1][2] map to source panel entries
6. If AI_GATEWAY_API_KEY is not provisioned, this is blocked — tell Claude "EVID-03 blocked: no API key"

**Item 2: TIMING_REVIEW.md closure**
1. Open `.planning/phases/07-timing-rules/TIMING_REVIEW.md` (if it exists)
2. Review any flagged safety-critical timing decisions
3. Approve or modify as needed
4. Tell Claude "TIMING_REVIEW approved" or provide specific changes

**Item 3: SQL manifest execution**
51 SQL files are pending in EXECUTION_MANIFEST.json. Execute them in dependency order against live Supabase:
1. Open Supabase Dashboard SQL editor
2. Execute files in manifest order (order 10 through 62), skipping status=applied files
3. After each batch, run the post_exec_verification query from the manifest entry
4. Tell Claude which files were executed, or "SQL execution deferred to later"

  </how-to-verify>
  <verify>Chris responds with status for each of the three items</verify>
  <acceptance_criteria>
    - Chris has responded with a status for EVID-03 (passed/blocked/deferred)
    - Chris has responded with a status for TIMING_REVIEW (approved/deferred)
    - Chris has responded with a status for SQL execution (executed/deferred)
  </acceptance_criteria>
  <resume-signal>
For each item, respond with one of:
- "EVID-03 passed" / "EVID-03 blocked: {reason}" / "EVID-03 deferred"
- "TIMING_REVIEW approved" / "TIMING_REVIEW deferred"
- "SQL executed through order {N}" / "SQL execution deferred"
  </resume-signal>
  <done>All three v1.1 carry-forward items have a documented status from Chris</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md and REQUIREMENTS.md with close-out results</name>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
Based on Chris's responses in Task 2, update both files:

**REQUIREMENTS.md updates:**
- If EVID-03 passed: change `- [ ] **EVID-03**:` to `- [x] **EVID-03**:` and update Traceability table status to `Complete`
- If EVID-03 blocked/deferred: leave as `- [ ]` and add note with blocker reason
- PAIR-01 already marked complete in Task 1

**STATE.md updates:**
- Under `### Pending Todos`, remove or update entries based on Chris's responses
- Under `### Blockers/Concerns`, update based on what was resolved vs deferred
- Add a new decision entry: `[v1.1 closeout]: CFRW-01 items resolved — {summary of what was completed/deferred}`
- Update `stopped_at:` to reflect Phase 11 progress
  </action>
  <verify>
    <automated>cd C:/projects/a360-v2 && grep "CFRW-01\|EVID-03\|PAIR-01" .planning/REQUIREMENTS.md && grep "v1.1 closeout" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/STATE.md contains a decision entry with text `v1.1 closeout`
    - .planning/REQUIREMENTS.md PAIR-01 line contains `[x]`
    - .planning/REQUIREMENTS.md EVID-03 line reflects Chris's actual response (either `[x]` or `[ ]` with note)
  </acceptance_criteria>
  <done>All v1.1 carry-forward items documented with final status in both STATE.md and REQUIREMENTS.md</done>
</task>

</tasks>

<verification>
- PAIR-01 verified via automated grep (0 contamination, file regenerated)
- EVID-03, TIMING_REVIEW, SQL execution statuses documented per Chris's responses
- REQUIREMENTS.md traceability table reflects current reality
- STATE.md pending todos updated
</verification>

<success_criteria>
All four CFRW-01 items have a documented resolution (complete, deferred with reason, or blocked with blocker). REQUIREMENTS.md and STATE.md updated to reflect.
</success_criteria>

<output>
After completion, create `.planning/phases/11-source-framework-and-v1.1-closeout/11-01-SUMMARY.md`
</output>
