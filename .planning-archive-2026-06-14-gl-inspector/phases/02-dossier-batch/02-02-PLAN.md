---
phase: 02-dossier-batch
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - compile_manifest.json
  - REVIEW_QUEUE/
autonomous: true
requirements: []

must_haves:
  truths:
    - "6 remaining category dossiers exist in agent_reference_docs as status=draft, version=1"
    - "Each category dossier has 4 doc rows (clinical_summary, technique_guide, category_overview, deep_dive_playbook)"
    - "Each category has evidence_links with authority_rank populated"
    - "source_registry has new rows for every reputable source encountered during category compile"
  artifacts:
    - path: "compile_manifest.json"
      provides: "Updated with category compile status=inserted and doc IDs"
    - path: "REVIEW_QUEUE/"
      provides: "Review files for each category dossier"
  key_links:
    - from: "agent_reference_docs (category rows)"
      to: "categories table"
      via: "category_id FK"
      pattern: "category_id"
    - from: "agent_reference_docs (product rows, later plans)"
      to: "agent_reference_docs (category rows)"
      via: "extends_doc_id"
      pattern: "extends_doc_id"
---

<objective>
Compile dossiers for the 6 remaining categories (HA Fillers, Biostimulators, Energy/RF, Energy/Laser, Skincare Actives, Body Contouring).

Purpose: Category dossiers are the parent layer — product dossiers inherit from them via extends_doc_id. All 6 must be compiled BEFORE product dossiers can proceed. This is the same pattern used for Neurotoxins (the calibration run).

Output: 24 agent_reference_docs rows (4 per category), evidence_links, source_registry entries, review queue files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dossier-batch/BATCH_SOURCE_LOGGING_ADDENDUM.md
@.planning/phases/02-dossier-batch/STRUCTURED_EMISSION_ADDENDUM.md
@.planning/phases/02-dossier-batch/AESTHETIC_DERMATOLOGY_JOURNAL_REGISTRY.md
@Fable Docs/DOSSIER_COMPILE_PIPELINE.md
@Fable Docs/DOSSIER_TEMPLATES.md
@compile_manifest.json

<interfaces>
<!-- Category dossier row structure (from DOSSIER_TEMPLATES.md) -->
Each category produces 4 rows in agent_reference_docs:
  1. lens=clinical, doc_type=clinical_summary, audience=provider
     Sections: class_overview, shared_mechanism, class_indications, class_candidacy, class_safety, member_differentiation
  2. lens=clinical, doc_type=technique_guide, audience=provider
     Sections: class_technique_principles [gateway posture], class_planning
  3. lens=sales_education, doc_type=category_overview, audience=patient  *(PRIMARY)*
     Sections: category_explainer, why_this_category, combination_therapy, cost_benefit_principles, category_objections
  4. lens=deep_product, doc_type=deep_dive_playbook, audience=provider
     Sections: category_landscape, selection_framework, evidence_base

<!-- Source capture spec (from BATCH_SOURCE_LOGGING_ADDENDUM.md) -->
For every reputable source encountered during compile:
  INSERT INTO source_registry: name, publisher, society, source_kind, authority_rank, rights_class, base_url, doi_prefix, added_by='discovery', status='review'
  If ingestible-looking (public_domain, open_access_cc_by, manufacturer_permitted):
    ALSO INSERT INTO ingestion_queue: status='queued'
  Deduplicate by name/doi_prefix before inserting.

<!-- Priority OA journals to aggressively surface (user instruction) -->
JCAD (Journal of Clinical and Aesthetic Dermatology) — OA on PMC, authority_rank 8/10
Cureus — OA, authority_rank 6/10
Dermatology and Therapy (Springer Adis) — OA CC-BY-NC, authority_rank 7/10
ASJ (Aesthetic Surgery Journal) — hybrid, authority_rank 9/10
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile 3 category dossiers — HA Fillers, Biostimulators, Body Contouring</name>
  <files>compile_manifest.json, REVIEW_QUEUE/</files>
  <read_first>
    - compile_manifest.json (get category IDs from Task 2 of Plan 01)
    - Fable Docs/DOSSIER_COMPILE_PIPELINE.md (the master compile prompt — section 0)
    - Fable Docs/DOSSIER_TEMPLATES.md (category dossier row structure)
    - Fable Docs/GATEWAY_POSTURE_ADDENDUM.md (clinical lens posture)
    - Fable Docs/SALES_EDUCATION_PRIORITY_ADDENDUM.md (sales_education is PRIMARY lens)
    - .planning/phases/02-dossier-batch/BATCH_SOURCE_LOGGING_ADDENDUM.md (source capture)
    - .planning/phases/02-dossier-batch/AESTHETIC_DERMATOLOGY_JOURNAL_REGISTRY.md (journal authority tiers)
    - REVIEW_QUEUE/PENDING_botox_neurotoxins_sales_education.md (style reference — the approved calibration)
  </read_first>
  <action>
    For EACH of the 3 categories (HA Fillers, Biostimulators, Body Contouring), execute the DOSSIER_COMPILE_PIPELINE.md master prompt (Steps 1-5):

    **Per category:**
    1. SCOPE: Resolve the category in `categories` table. Get member products via `item_categories`.
    2. GATHER: Retrieve claim cards from structured taxonomy, existing evidence_links, PubMed (keyword search in CMS vector project), FDA labels, YouTube/podcast sources.
    3. DEDUPE & CORROBORATE: Cluster claims, assign authority ranks, enforce safety floor rules.
    4. COMPILE THREE LENSES per DOSSIER_TEMPLATES.md category structure:
       - clinical (clinical_summary + technique_guide) — gateway posture, inline tier tags, no precise dosing tables
       - sales_education (category_overview) — PRIMARY lens, deepest effort, combination_therapy, cost_benefit_principles, category_objections
       - deep_product (deep_dive_playbook) — provider-grade differentiation
    5. EMIT: INSERT 4 agent_reference_docs rows per category (status='draft', version=1). INSERT evidence_links for every asserted claim.

    **Source capture (per BATCH_SOURCE_LOGGING_ADDENDUM):**
    For every reputable source encountered:
    - Check source_registry by name/doi_prefix. If not present, INSERT with added_by='discovery', status='review'.
    - If source is OA/public_domain, ALSO insert ingestion_queue row with status='queued'.
    - AGGRESSIVELY surface OA articles from: JCAD, Cureus, Dermatology and Therapy, ASJ. When you encounter articles from these journals, always log them to source_registry even if only tangentially relevant.

    **Compile report:** For each category, write a REVIEW_QUEUE file:
    - `REVIEW_QUEUE/PENDING_{category_slug}_sales_education.md`
    - `REVIEW_QUEUE/PENDING_{category_slug}_clinical.md`

    **Update compile_manifest.json** with inserted doc IDs and status='inserted' for each category.

    **Posture reminders (from calibration run):**
    - Gateway posture: characterize in ranges, point to source for exact protocol. Safety floor is authoritative.
    - sales_education is PRIMARY: deepest compile effort. combination_therapy section is key deliverable.
    - No personal names from podcast/youtube content.
    - No item_relationships rows (pairings are a future phase).
    - All writes land as draft. Nothing set to active.
  </action>
  <verify>
    <automated>
    Run against Supabase project aejskvmpembryunnbgrk:
    SELECT c.name, COUNT(ard.id) as doc_count
    FROM categories c
    JOIN agent_reference_docs ard ON ard.category_id = c.id
    WHERE ard.status = 'draft'
    AND c.name IN ('HA Fillers', 'Biostimulators', 'Body Contouring')
    GROUP BY c.name;
    -- expect 3 rows, each with doc_count = 4

    SELECT COUNT(*) FROM source_registry WHERE added_by = 'discovery' AND status = 'review';
    -- expect > 14 (was 1 before batch, should grow)
    </automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 categories has exactly 4 agent_reference_docs rows (clinical_summary, technique_guide, category_overview, deep_dive_playbook) with status='draft'
    - Each clinical doc has at least 1 evidence_links row with authority_rank <= 4
    - source_registry has new discovery rows (count > what existed before)
    - REVIEW_QUEUE/ has files for each category
    - compile_manifest.json updated with doc IDs and status='inserted' for all 3 categories
    - No personal names in any content_md (verified by regex: content_md !~ '\m(Dr\.?|Doctor)\s+[A-Z][a-z]+')
  </acceptance_criteria>
  <done>HA Fillers, Biostimulators, Body Contouring category dossiers compiled with evidence, source capture logged, review files written</done>
</task>

<task type="auto">
  <name>Task 2: Compile 3 category dossiers — Energy/RF, Energy/Laser, Skincare Actives</name>
  <files>compile_manifest.json, REVIEW_QUEUE/</files>
  <read_first>
    - compile_manifest.json (get category IDs)
    - Fable Docs/DOSSIER_COMPILE_PIPELINE.md (master compile prompt)
    - Fable Docs/DOSSIER_TEMPLATES.md (category structure)
    - Fable Docs/GATEWAY_POSTURE_ADDENDUM.md
    - Fable Docs/SALES_EDUCATION_PRIORITY_ADDENDUM.md
    - .planning/phases/02-dossier-batch/BATCH_SOURCE_LOGGING_ADDENDUM.md
    - .planning/phases/02-dossier-batch/AESTHETIC_DERMATOLOGY_JOURNAL_REGISTRY.md
  </read_first>
  <action>
    Same process as Task 1, for the remaining 3 categories: Energy/RF, Energy/Laser, Skincare Actives.

    Execute DOSSIER_COMPILE_PIPELINE.md Steps 1-5 for each category.

    **Special notes for energy categories:**
    - Energy/RF and Energy/Laser are device categories — the clinical lens should focus on mechanism of action (RF vs ablative vs non-ablative vs fractional), treatment parameters (energy, pulse width, cooling), and safety profile.
    - Source capture for energy devices: prioritize Lasers in Surgery and Medicine, Journal of Cosmetic and Laser Therapy, Dermatologic Surgery — these are the key journals per the AESTHETIC_DERMATOLOGY_JOURNAL_REGISTRY.md.

    **Special notes for Skincare Actives:**
    - This category may be thinner than injectables/devices. Focus on mechanism (retinoids, antioxidants, peptides, growth factors) and evidence hierarchy.
    - If the category doesn't exist in the DB, create it or note the gap in the summary.

    **Source capture:** Same aggressive OA surfacing as Task 1 — JCAD, Cureus, Dermatology and Therapy, ASJ. Log every reputable laser/energy/skincare journal source to source_registry.

    **Write review queue files and update compile_manifest.json.**
  </action>
  <verify>
    <automated>
    Run against Supabase:
    SELECT c.name, COUNT(ard.id) as doc_count
    FROM categories c
    JOIN agent_reference_docs ard ON ard.category_id = c.id
    WHERE ard.status = 'draft'
    AND c.name NOT IN ('Neurotoxins', 'HA Fillers', 'Biostimulators', 'Body Contouring')
    GROUP BY c.name;
    -- expect 3 rows, each with doc_count = 4
    </automated>
  </verify>
  <acceptance_criteria>
    - Energy/RF, Energy/Laser, Skincare Actives each have 4 agent_reference_docs rows with status='draft'
    - Each clinical doc has evidence_links with authority_rank populated
    - source_registry has new rows for laser/energy journals (Lasers in Surgery and Medicine, JCLT, etc.)
    - REVIEW_QUEUE/ files written for each
    - compile_manifest.json updated
  </acceptance_criteria>
  <done>All 6 remaining category dossiers compiled — the parent layer is complete for product inheritance</done>
</task>

</tasks>

<verification>
Run against Supabase project aejskvmpembryunnbgrk:
```sql
-- All 7 categories (including Neurotoxins) should have dossiers
SELECT c.name,
  (SELECT COUNT(*) FROM agent_reference_docs ard WHERE ard.category_id = c.id AND ard.status IN ('draft','corrected')) as docs
FROM categories c
WHERE c.name IN ('Neurotoxins','HA Fillers','Biostimulators','Body Contouring','Energy/RF','Energy/Laser','Skincare Actives')
ORDER BY c.name;
-- Expect: 7 rows, each with docs >= 4

-- Source registry growth
SELECT COUNT(*) FROM source_registry;
-- Expect: significantly more than 14 (starting count)
```
</verification>

<success_criteria>
All 7 category dossiers (including the already-done Neurotoxins) exist in agent_reference_docs. Product dossiers in Plans 03-05 can now inherit via extends_doc_id. Source capture is logging every reputable source encountered.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dossier-batch/02-02-SUMMARY.md`
</output>
