---
phase: 02-dossier-batch
plan: 05
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - compile_manifest.json
  - REVIEW_QUEUE/
autonomous: true
requirements: []

must_haves:
  truths:
    - "All energy/device products (Morpheus8, Thermage, etc.) have 5 agent_reference_docs rows (status=draft)"
    - "Each energy product has item_concerns, item_body_areas, aliases, and does_not_solve populated"
    - "Energy/RF products inherit from Energy/RF category; Energy/Laser from Energy/Laser category"
    - "Skincare active products (if any) have dossiers and structured emission"
  artifacts:
    - path: "compile_manifest.json"
      provides: "Updated with energy + skincare product compile status"
  key_links:
    - from: "agent_reference_docs (energy products)"
      to: "agent_reference_docs (Energy/RF or Energy/Laser category)"
      via: "extends_doc_id"
      pattern: "extends_doc_id"
---

<objective>
Compile product dossiers + structured emission for Energy/RF products (Morpheus8, Thermage, Vivace), Energy/Laser products (PicoSure, Fraxel, Clear+Brilliant, BBL), and any Skincare Actives products.

Purpose: Energy-based devices are a growing segment in aesthetic medicine. These products have different mechanisms (RF microneedling, monopolar RF, fractional laser, picosecond laser, IPL/BBL) requiring distinct clinical and sales framing.

Output: ~25-40 agent_reference_docs rows, structured intelligence, source capture (especially laser/energy journals).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dossier-batch/STRUCTURED_EMISSION_ADDENDUM.md
@.planning/phases/02-dossier-batch/BATCH_SOURCE_LOGGING_ADDENDUM.md
@.planning/phases/02-dossier-batch/AESTHETIC_DERMATOLOGY_JOURNAL_REGISTRY.md
@Fable Docs/DOSSIER_COMPILE_PIPELINE.md
@Fable Docs/DOSSIER_TEMPLATES.md
@compile_manifest.json
@.planning/phases/02-dossier-batch/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile Energy/RF + Energy/Laser product dossiers</name>
  <files>compile_manifest.json, REVIEW_QUEUE/</files>
  <read_first>
    - compile_manifest.json (get product IDs for energy devices)
    - .planning/phases/02-dossier-batch/02-02-SUMMARY.md (Energy/RF and Energy/Laser category doc IDs for extends_doc_id)
    - Fable Docs/DOSSIER_COMPILE_PIPELINE.md
    - Fable Docs/DOSSIER_TEMPLATES.md
    - .planning/phases/02-dossier-batch/STRUCTURED_EMISSION_ADDENDUM.md
    - .planning/phases/02-dossier-batch/BATCH_SOURCE_LOGGING_ADDENDUM.md
    - .planning/phases/02-dossier-batch/AESTHETIC_DERMATOLOGY_JOURNAL_REGISTRY.md (Tier 2 laser journals)
  </read_first>
  <action>
    For EACH energy device product, execute DOSSIER_COMPILE_PIPELINE.md Steps 1-5:

    **Energy/RF products** (inherit from Energy/RF category):
    - Morpheus8 (fractional RF microneedling): skin tightening, texture, scarring, subdermal remodeling
    - Thermage (monopolar RF): skin tightening, collagen stimulation, non-invasive
    - Vivace (RF microneedling): skin tightening, fine lines, texture

    **Energy/Laser products** (inherit from Energy/Laser category):
    - PicoSure (picosecond laser): pigmentation, tattoo removal, skin revitalization
    - Fraxel (fractional laser): skin resurfacing, scarring, pigmentation, fine lines
    - Clear+Brilliant (gentle fractional laser): mild resurfacing, pore refinement, preventative
    - BBL (broadband light/IPL): pigmentation, redness, sun damage, skin quality

    **Device-specific compile notes:**
    - Energy devices have DIFFERENT safety profiles than injectables — burns, post-inflammatory hyperpigmentation (PIH), Fitzpatrick skin type considerations are critical safety floor items.
    - Treatment parameters (energy levels, pulse widths, tip sizes) should follow gateway posture: characterize in ranges, point to manufacturer IFU for exact settings. No precise parameter tables.
    - Combination with injectables: reference the corrected Botox combination_sequencing from the calibration run (energy first, then filler; BoNT same-day OK for standard devices).

    **Structured emission per product:**

    a) item_concerns:
       - Morpheus8: Skin Laxity (primary), Acne Scarring (primary), Skin Texture (primary), Fine Lines (secondary), Stretch Marks (adjunctive), Submental Fullness (secondary)
       - Thermage: Skin Laxity (primary, FDA-cleared), Wrinkles (primary), Jawline Definition (secondary), Eyelid Laxity (primary, FDA-cleared)
       - PicoSure: Pigmentation (primary, FDA-cleared), Tattoo Removal (primary, FDA-cleared), Skin Revitalization (secondary)
       - Fraxel: Acne Scarring (primary), Pigmentation (primary), Fine Lines (primary), Surgical Scars (primary), Photodamage (primary)
       - Clear+Brilliant: Fine Lines (primary), Pore Size (primary), Skin Texture (primary), Melasma (adjunctive, low settings)
       - BBL: Pigmentation (primary, FDA-cleared), Redness/Rosacea (primary), Sun Damage (primary), Skin Quality (secondary)

    b) item_body_areas: Face (area), Neck (area), Chest/Decolletage (area for BBL/Fraxel), Hands (area for BBL), specific zones where applicable.

    c) aliases: "brown spots" -> Pigmentation, "age spots" -> Sun Damage, "red face" -> Redness/Rosacea, "acne scars" -> Acne Scarring, "loose skin" -> Skin Laxity, "large pores" -> Pore Size, "sun damage" -> Photodamage, etc.

    d) does_not_solve per product (examples):
       - Morpheus8: ["volume loss", "dynamic expression lines", "deep structural hollowing", "pigmentation (limited)", "vascular lesions"]
       - BBL: ["volume loss", "deep wrinkles", "skin laxity (structural)", "dynamic lines", "deep scarring"]
       - PicoSure: ["volume loss", "skin laxity", "dynamic lines", "deep wrinkles", "vascular concerns"]

    **Source capture — PRIORITY for energy devices:**
    The AESTHETIC_DERMATOLOGY_JOURNAL_REGISTRY.md lists these as key laser/energy journals:
    - Lasers in Surgery and Medicine (Wiley, authority_rank 8/10)
    - Journal of Cosmetic and Laser Therapy (Taylor & Francis, authority_rank 7/10)
    - Dermatologic Surgery (LWW, authority_rank 8/10)
    Aggressively log sources from these journals. Also surface OA content from Cureus, Dermatology and Therapy, JCAD about energy-based devices.

    **Taxonomy audit + compile_manifest update + REVIEW_QUEUE files.**
  </action>
  <verify>
    <automated>
    Run against Supabase:
    SELECT p.name, COUNT(ard.id) as docs,
      (SELECT COUNT(*) FROM item_concerns ic WHERE ic.offering_id=p.id) as concerns,
      (SELECT COUNT(*) FROM item_body_areas iba WHERE iba.offering_id=p.id) as body_areas,
      COALESCE(array_length(p.does_not_solve,1),0) as limitations
    FROM products p
    JOIN agent_reference_docs ard ON ard.offering_id = p.id AND ard.status='draft'
    WHERE p.name ILIKE ANY(ARRAY['%morpheus%','%thermage%','%vivace%','%picosure%','%fraxel%','%clear+brilliant%','%bbl%'])
    GROUP BY p.id, p.name, p.does_not_solve;
    -- expect: each has docs=5, concerns>=3, body_areas>=1, limitations>=3
    </automated>
  </verify>
  <acceptance_criteria>
    - Each energy device product has 5 agent_reference_docs rows with status='draft'
    - RF products have extends_doc_id to Energy/RF category; laser products to Energy/Laser category
    - Each has >= 3 item_concerns
    - Safety sections include Fitzpatrick considerations and PIH risk where applicable
    - source_registry has new entries from Lasers in Surgery and Medicine, JCLT, Derm Surg
    - does_not_solve populated for each
  </acceptance_criteria>
  <done>All energy device products compiled with dossiers, structured intelligence, and source capture</done>
</task>

<task type="auto">
  <name>Task 2: Compile Skincare Actives products (if any in DB) + Dysport product dossier</name>
  <files>compile_manifest.json, REVIEW_QUEUE/</files>
  <read_first>
    - compile_manifest.json (check for skincare active products + Dysport)
    - .planning/phases/02-dossier-batch/02-02-SUMMARY.md (category doc IDs)
    - Fable Docs/DOSSIER_COMPILE_PIPELINE.md
    - Fable Docs/DOSSIER_TEMPLATES.md
    - .planning/phases/02-dossier-batch/STRUCTURED_EMISSION_ADDENDUM.md
    - OVERNIGHT_REPORT.md (section 5.5 — Dysport data is sparse, needs enrichment)
  </read_first>
  <action>
    **Part A — Skincare Actives:**
    Query the products table for any products in the Skincare Actives category:
    ```sql
    SELECT p.id, p.name FROM products p
    JOIN item_categories ic ON ic.offering_id = p.id
    JOIN categories c ON c.id = ic.category_id
    WHERE c.slug ILIKE '%skincare%' OR c.slug ILIKE '%actives%';
    ```

    If products exist: compile dossiers using the same pipeline. Skincare actives have weaker evidence than FDA-approved injectables — be honest about evidence tiers. Many claims rest on in-vitro studies (authority_rank 5-6).

    If NO skincare active products exist in the DB: note the gap in the summary. Do NOT create phantom products. This is expected — skincare may be a post-demo addition.

    **Part B — Dysport product dossier:**
    Dysport (abobotulinumtoxinA) is in the Neurotoxins category (already compiled). The OVERNIGHT_REPORT noted Dysport has sparse structured data (only form + route populated).

    1. First, enrich Dysport's structured taxonomy fields from its FDA prescribing information:
    ```sql
    -- Check current state
    SELECT name, onset_time, peak_effect, duration_of_effect, indications, contraindications,
           fda_approved_areas FROM products WHERE name ILIKE '%dysport%';
    ```

    2. If fields are NULL, populate from FDA label data (research the prescribing information):
       - onset_time: "2-3 days" (typical for abobotulinumtoxinA)
       - peak_effect: "14 days"
       - duration_of_effect: "3-4 months"
       - indications: array of FDA-approved indications
       - fda_approved_areas: array of FDA-cleared treatment areas
       - contraindications: from prescribing information

    3. Then compile the Dysport dossier using DOSSIER_COMPILE_PIPELINE.md, inheriting from Neurotoxins category via extends_doc_id.

    4. Structured emission for Dysport — similar to Botox but with Dysport-specific differences:
       - item_concerns: Glabellar Lines (primary, FDA-indicated), Cervical Dystonia (primary, FDA-indicated), etc.
       - item_body_areas: Glabella, Forehead, Crow's Feet area, etc.
       - does_not_solve: similar to Botox (no volume, no static lines, no skin quality, etc.)
       - aliases: same concern aliases as Botox where applicable

    **Any remaining products** not covered by Plans 03-05 Task 1: compile them here using the appropriate category parent.

    **Source capture + taxonomy audit + compile_manifest + REVIEW_QUEUE as always.**
  </action>
  <verify>
    <automated>
    Run against Supabase:
    -- Dysport should be compiled
    SELECT p.name, COUNT(ard.id) as docs,
      (SELECT COUNT(*) FROM item_concerns ic WHERE ic.offering_id=p.id) as concerns,
      COALESCE(array_length(p.does_not_solve,1),0) as limitations
    FROM products p
    LEFT JOIN agent_reference_docs ard ON ard.offering_id = p.id AND ard.status='draft'
    WHERE p.name ILIKE '%dysport%'
    GROUP BY p.id, p.name, p.does_not_solve;
    -- expect: docs=5, concerns>=3, limitations>=4

    -- All pending products should now be compiled
    SELECT COUNT(*) FROM products p
    WHERE p.kind='product'
    AND NOT EXISTS (SELECT 1 FROM agent_reference_docs ard WHERE ard.offering_id=p.id AND ard.status='draft');
    -- expect: 0 (every product has at least one draft doc)
    </automated>
  </verify>
  <acceptance_criteria>
    - Dysport has 5 agent_reference_docs rows with status='draft' and extends_doc_id to Neurotoxins category
    - Dysport structured taxonomy fields (onset_time, peak_effect, duration_of_effect) are no longer NULL
    - Dysport has item_concerns, item_body_areas, does_not_solve populated
    - If skincare active products exist: compiled with dossiers
    - If no skincare active products: gap noted in summary
    - All products in compile_manifest.json have status='inserted'
    - No remaining products without draft dossiers
  </acceptance_criteria>
  <done>All remaining products compiled — the entire product batch is complete</done>
</task>

</tasks>

<verification>
```sql
-- MASTER CHECK: every product should have dossier docs
SELECT p.name,
  (SELECT COUNT(*) FROM agent_reference_docs ard WHERE ard.offering_id=p.id AND ard.status IN ('draft','corrected')) AS docs,
  (SELECT COUNT(*) FROM item_concerns ic WHERE ic.offering_id=p.id) AS concerns,
  (SELECT COUNT(*) FROM item_body_areas iba WHERE iba.offering_id=p.id) AS body_areas,
  COALESCE(array_length(p.does_not_solve,1),0) AS limitations
FROM products p
WHERE p.kind='product'
ORDER BY p.name;
-- Expect: NO zeros in docs or limitations for any product
```
</verification>

<success_criteria>
All energy device products, skincare actives (if any), and Dysport are compiled. Combined with Plans 03 and 04, the entire product batch is complete. Every product has dossiers, structured intelligence, and source capture.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dossier-batch/02-05-SUMMARY.md`
</output>
