---
phase: 07-timing-rules
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - supabase/compile_sql/07-03-pair-timing.sql
  - .planning/phases/07-timing-rules/TIMING_EVALUATION.md
  - .planning/phases/07-timing-rules/TIMING_REVIEW.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "~30 canonical/common pairs have same_session_ok, staging, and safety data populated"
    - "All safety-critical pairs (hard_block and warning) are flagged with safety_critical=true"
    - "TIMING_REVIEW.md lists all items requiring Chris review with recommended defaults"
    - "TIMING_EVALUATION.md provides full timing rule inventory with evidence levels"
  artifacts:
    - path: "supabase/compile_sql/07-03-pair-timing.sql"
      provides: "UPDATE statements for ~30 item_relationships rows with timing data"
      contains: "UPDATE item_relationships SET"
    - path: ".planning/phases/07-timing-rules/TIMING_EVALUATION.md"
      provides: "Full timing rule inventory with evidence sources and confidence levels"
      contains: "Evidence-Backed Timing Rules"
    - path: ".planning/phases/07-timing-rules/TIMING_REVIEW.md"
      provides: "Safety-critical items for Chris review"
      contains: "safety_critical"
  key_links:
    - from: "supabase/compile_sql/07-03-pair-timing.sql"
      to: "item_relationships"
      via: "UPDATE by item_a_id + item_b_id"
      pattern: "UPDATE item_relationships SET.*WHERE"
---

<objective>
Populate pair-level timing data for ~30 canonical/common item_relationships rows and produce evaluation + review documentation.

Purpose: Complete the timing data layer so agents can answer same-session compatibility, sequencing, and safety questions from structured data. Produce review artifacts for Chris to validate safety-critical rules.
Output: Pair timing UPDATE SQL + TIMING_EVALUATION.md + TIMING_REVIEW.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-timing-rules/07-CONTEXT.md
@.planning/phases/07-timing-rules/PHASE_7_TIMING_RESEARCH_SPEC.md
@.planning/phases/07-timing-rules/07-01-SUMMARY.md
@.planning/phases/06-pairing-engine/PAIRING_RUBRIC.md

<interfaces>
<!-- Phase 6 creates item_relationships rows with relationship_type, relationship_strength, pairing_tier, clinical_rationale, timing_guidance, same_session_ok (text in timing_guidance), patient_education_text, staff_talking_points -->
<!-- Phase 7 Plan 01 adds: same_session_ok (BOOLEAN), same_session_rationale, staging_required, staging_sequence, staging_interval_days_min, staging_interval_days_max, staging_rationale, combined_downtime_note, safety_critical, timing_warning_level, timing_notes -->
<!-- Pair UPDATEs must target existing item_relationships rows using item_a_id + item_b_id WHERE clause -->
<!-- item_a_id and item_b_id reference gl_products.offering_id -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pair timing UPDATEs for canonical/common item_relationships</name>
  <files>supabase/compile_sql/07-03-pair-timing.sql</files>
  <read_first>
    - .planning/phases/07-timing-rules/PHASE_7_TIMING_RESEARCH_SPEC.md (Same-Session Matrix, Sequencing Matrix, Contraindication Matrix — exact data)
    - .planning/phases/07-timing-rules/07-CONTEXT.md (D-1 default YES, D-2 exception model, D-7 sequencing fields, D-11 safety_critical, D-12 hybrid safety)
    - .planning/phases/06-pairing-engine/DOES_NOT_SOLVE_BACKFILL.md (offering_ids for all 18 products)
    - supabase/compile_sql/07-01-timing-schema.sql (verify column names from Plan 01)
  </read_first>
  <action>
Create `supabase/compile_sql/07-03-pair-timing.sql` with UPDATE statements for pair-level timing data.

**IMPORTANT:** Phase 6 may not have created all item_relationships rows yet (Phase 6 is still executing plans 02-05). The SQL must use `UPDATE ... WHERE item_a_id = X AND item_b_id = Y` — if the row doesn't exist yet, the UPDATE is a no-op and can be re-run after Phase 6 completes. Add a comment at the top explaining this.

**CRITICAL:** item_relationships uses `item_a_id` and `item_b_id` — these correspond to `offering_id` values from gl_products. The pair direction matters: use alphabetical ordering by product name to match Phase 6's pair generation convention. If unsure of direction, include both orderings in the WHERE clause with OR.

**Pair-to-timing mapping (from research spec matrices):**

The file should contain UPDATE statements organized by category:

**Section 1: Same-Session Compatibility (default posture = YES per D-1)**
For each pair listed in the Same-Session Compatibility Matrix, set:
- same_session_ok = true/false (per matrix)
- same_session_rationale = clinical rationale text
- For conditional pairs: same_session_ok = true with rationale noting conditions

Key pairs to include (using offering_ids from DOES_NOT_SOLVE_BACKFILL.md):

Neurotoxin + HA Filler pairs (all YES, PubMed HIGH):
- Botox + each HA filler (Voluma, Vollure, SKINVIVE, Restylane Lyft, RHA Redensity)
- Dysport + each HA filler
- (5 toxins x 5 fillers = 25 pairs, all same_session_ok=true)

Neurotoxin + Biostimulator (YES):
- Each toxin + Sculptra (5 pairs)

Neurotoxin + Energy (YES, PubMed MODERATE):
- Each toxin + Morpheus8, Sofwave, Ultherapy, Hollywood Spectra

HA Filler + Biostimulator (YES):
- Each filler + Sculptra

HA Filler + RF Microneedling (YES, expert consensus):
- Each filler + Morpheus8

PRP/Biostimulator + RF/Energy (YES, PubMed HIGH for PRP+laser):
- Sculptra + Morpheus8

**Section 2: Sequencing Rules (per D-7)**
For pairs with sequencing requirements, set:
- staging_required = true
- staging_sequence = 'product_a_first' or 'product_b_first' (relative to alphabetical item_a/item_b ordering)
- staging_interval_days_min / staging_interval_days_max
- staging_rationale = clinical explanation

Key sequencing pairs:
- Sculptra -> HA Filler: Sculptra first (collagen foundation), 4-8 weeks between
- BoNT-A -> Laser (Ultherapy/Spectra): BoNT-A 1 week before (PubMed HIGH RCT)

**Section 3: Safety-Critical Pairs (per D-11, D-12)**
For safety-critical pairs, set:
- safety_critical = true
- timing_warning_level = 'hard_block' or 'warning' or 'education'
- timing_notes = explanation including evidence source

Hard blocks (~3-4):
- (Hyaluronidase pairs are not in our 18-product manifest, so no hyaluronidase hard block row needed)
- Note: The hard blocks from the research spec (hyaluronidase+HA, ablative laser+isotretinoin, BoNT-A in pregnancy) are product-level contraindications or involve products not in the 18-product manifest. Document in TIMING_EVALUATION.md instead.

Warnings (~5-8):
- Any pair where >50U toxin per session is likely (document in timing_notes, not as pair-level restriction)
- Rejuran + cross-linked filler (not in manifest — document in evaluation)

Education notes:
- Filler + RF microneedling: same_session_ok=true, timing_notes captures expert disagreement
- CoolSculpting retreatment timing in product-level cadence_notes (already done in Plan 01)

**Section 4: Combined Downtime Notes (per D-10)**
For canonical/common pairs, set combined_downtime_note where expert-determined guidance exists:
- Default combined downtime = max(individual) — document this default in file header
- Special: PRP + RF microneedling → downtime REDUCED (PRP accelerates healing)

**Approach:** Rather than enumerating all 153 pairs, focus on the ~30 most common pairs that have specific timing rules. For the remainder, the default posture (same_session_ok not set = default YES per D-1) applies. The file should UPDATE only pairs where a specific rule exists.

Add a verification query at the end:
```sql
-- Verification: Count pairs with timing data populated
SELECT COUNT(*) as pairs_with_timing,
  COUNT(CASE WHEN safety_critical THEN 1 END) as safety_critical_count,
  COUNT(CASE WHEN timing_warning_level = 'hard_block' THEN 1 END) as hard_blocks,
  COUNT(CASE WHEN timing_warning_level = 'warning' THEN 1 END) as warnings,
  COUNT(CASE WHEN staging_required THEN 1 END) as staging_required_count
FROM item_relationships
WHERE same_session_ok IS NOT NULL
  OR staging_required = true
  OR safety_critical = true;
```
  </action>
  <verify>
    <automated>grep -c "UPDATE item_relationships SET" supabase/compile_sql/07-03-pair-timing.sql && echo "UPDATE count above"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/compile_sql/07-03-pair-timing.sql contains at least 15 "UPDATE item_relationships SET" statements
    - File contains at least 1 row with safety_critical = true and timing_warning_level = 'warning'
    - File contains at least 2 rows with staging_required = true
    - All offering_ids used in WHERE clauses come from the 18 manifest products
    - No "podcast" or "episode" references in SQL string literals
    - File header explains Phase 6 dependency and re-run safety
    - File ends with a verification SELECT query
    - same_session_rationale values reference evidence level (e.g., "PubMed HIGH" or "expert consensus") without podcast attribution
  </acceptance_criteria>
  <done>Pair timing SQL contains UPDATE statements for canonical/common pairs with same-session, sequencing, safety, and combined downtime data. At least 15 pairs covered. Safety-critical flags set. Verification query included.</done>
</task>

<task type="auto">
  <name>Task 2: TIMING_EVALUATION.md and TIMING_REVIEW.md documentation</name>
  <files>.planning/phases/07-timing-rules/TIMING_EVALUATION.md, .planning/phases/07-timing-rules/TIMING_REVIEW.md</files>
  <read_first>
    - .planning/phases/07-timing-rules/PHASE_7_TIMING_RESEARCH_SPEC.md (Timing Rule Inventory table — rules 1-34)
    - .planning/phases/07-timing-rules/07-CONTEXT.md (Chris Review Items section — 7 items)
    - supabase/compile_sql/07-02-product-cadence.sql (verify product data matches evaluation)
    - supabase/compile_sql/07-03-pair-timing.sql (verify pair data matches evaluation)
  </read_first>
  <action>
Create two documentation files:

**TIMING_EVALUATION.md** — Full timing rule inventory. Structure:

```markdown
# Phase 7: Timing Rule Evaluation

## Summary
- Products with cadence data: 18/18
- Pairs with explicit timing rules: ~N (from pair SQL)
- Safety-critical rules: N hard blocks, N warnings, N education
- Evidence levels: N HIGH, N MODERATE, N LOW

## Product Cadence Rules
| Product | Series | Initial Interval | Maintenance | Min Retreat | Downtime | Evidence |
(One row per product, 18 rows, from 07-02-product-cadence.sql)

## Pair Timing Rules
| Pair | Same Session | Staging | Safety | Warning Level | Evidence |
(One row per pair with explicit rules, from 07-03-pair-timing.sql)

## Same-Session Compatibility Summary
- Default posture: YES (per D-1)
- Pairs with explicit YES: N
- Pairs with explicit NO: N
- Pairs with CONDITIONAL: N

## Sequencing Rules Summary
(List all pairs with staging_required=true)

## Safety-Critical Rules
| Rule | Type | Hardness | Evidence | In Schema? |
(Include ALL 34 rules from research spec, noting which are in-schema vs documented-only)

## Evidence Gaps
(From research spec: body contouring timing, skincare sequencing, combined downtime data)

## Deferred Items
- Event-based timeline calculator -> Phase 9
- Area-specific cadence table -> Phase 9
- Agent fuel compilation -> Phase 10
```

**TIMING_REVIEW.md** — Items requiring Chris review. Structure:

```markdown
# Phase 7: Timing Rules — Chris Review

## How to Use This Document
Review each item below. The recommended default is already implemented in SQL.
Mark each item: APPROVED (keep default) or OVERRIDE (specify change).

## Safety-Critical Review Items

### 1. Default same-session posture = YES (D-1)
**Current implementation:** same_session_ok defaults to YES when no explicit rule exists.
**Evidence:** PubMed RCT (n=90, BoNT-A + HA filler), podcast consensus, ASDS 2024 conference themes.
**Risk:** Business/liability decision. Default YES is commercially aligned and evidence-supported.
**Decision needed:** [ ] APPROVED  [ ] OVERRIDE: ___

### 2. >50 units toxin per session warning (D-12)
**Current implementation:** Documented in cadence_notes as warning. NOT a system hard block.
**Evidence:** Published study (Dr. Wanitphakdeedecha) shows increased antibody risk. Many practitioners routinely exceed 50U.
**Risk:** Too conservative for practitioner audience? Or appropriate caution?
**Decision needed:** [ ] APPROVED  [ ] OVERRIDE: ___

### 3. France filler+toxin prohibition
**Current implementation:** NOT in schema. Documented in TIMING_EVALUATION.md as out of scope.
**Evidence:** Legal prohibition in France (Dr. Martschin, Ep 169).
**Risk:** Regional legal restrictions are a separate feature. Phase 7 scope is clinical timing.
**Decision needed:** [ ] APPROVED (out of scope)  [ ] OVERRIDE: add regional flag

### 4. Downtime framing — conservative vs real-world
**Current implementation:** Both numbers captured. downtime_days_min/max uses published ranges. downtime_description includes real-world context.
**Evidence:** PubMed: CO2 ablative 7-10 days. YouTube experts with exosomes: 5 days.
**Decision needed:** [ ] APPROVED  [ ] OVERRIDE: ___

### 5. Filler + laser same-day
**Current implementation:** same_session_ok = true with education note about expert disagreement.
**Evidence:** Dr. Weiner: safe (HA sterilized at 120C vs 40-45C from RF). Some providers: wait 1-2 weeks. PubMed: no evidence of harm.
**Decision needed:** [ ] APPROVED  [ ] OVERRIDE: change to CONDITIONAL

### 6. Body contouring pairs — thin evidence
**Current implementation:** Included with evidence level noted. CoolSculpting + Kybella timing based on individual product cadence.
**Evidence:** Limited PubMed + podcast coverage for body contouring combinations.
**Decision needed:** [ ] APPROVED  [ ] OVERRIDE: defer to future phase

### 7. Full schema (22 fields) selected (D-14)
**Current implementation:** All 22 fields added. All nullable. No existing data affected.
**Alternative:** 11 essential fields would cover 80% of agent query needs.
**Decision needed:** [ ] APPROVED  [ ] OVERRIDE: reduce to 11 fields

## Pair-Specific Safety Review

### Rejuran + Cross-Linked HA Filler
**Rule:** Do not inject Rejuran where cross-linked filler exists in same plane.
**Status:** NOT in schema (Rejuran not in 18-product manifest). Documented in TIMING_EVALUATION.md.
**Source:** Dr. Steven Liew, Ep 302.
**Decision needed:** [ ] APPROVED (document only)  [ ] OVERRIDE: ___

### France Legal Restriction
**Rule:** Filler + BoNT-A same session prohibited in France.
**Status:** NOT in schema. Regional legal restriction = separate feature.
**Decision needed:** [ ] APPROVED (out of scope)  [ ] OVERRIDE: ___
```
  </action>
  <verify>
    <automated>test -f ".planning/phases/07-timing-rules/TIMING_EVALUATION.md" && test -f ".planning/phases/07-timing-rules/TIMING_REVIEW.md" && echo "PASS: both docs exist" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - TIMING_EVALUATION.md exists and contains "Product Cadence Rules" table with 18 rows
    - TIMING_EVALUATION.md contains "Pair Timing Rules" table
    - TIMING_EVALUATION.md contains "Safety-Critical Rules" section
    - TIMING_EVALUATION.md contains "Evidence Gaps" section
    - TIMING_EVALUATION.md contains "Deferred Items" section referencing Phase 9 and Phase 10
    - TIMING_REVIEW.md exists and contains at least 7 numbered review items
    - TIMING_REVIEW.md contains "APPROVED" and "OVERRIDE" checkboxes for each item
    - TIMING_REVIEW.md contains "Rejuran" safety review item
    - TIMING_REVIEW.md contains "France" legal restriction item
    - Neither file references "podcast" in source attribution (use "expert consensus" or "clinical practice data")
  </acceptance_criteria>
  <done>TIMING_EVALUATION.md provides complete timing rule inventory (18 products, ~30 pairs, all safety rules). TIMING_REVIEW.md lists 7+ review items with APPROVED/OVERRIDE checkboxes for Chris. No podcast attribution in either document.</done>
</task>

</tasks>

<verification>
- grep -c "UPDATE item_relationships SET" supabase/compile_sql/07-03-pair-timing.sql returns at least 15
- TIMING_EVALUATION.md contains product table with 18 rows
- TIMING_REVIEW.md contains at least 7 review items
- No "podcast" in SQL string literals or documentation source attributions
- All offering_ids used match the 18 manifest products
</verification>

<success_criteria>
- Pair timing SQL covers ~30 canonical/common pairs with same-session, sequencing, safety, and downtime data
- Safety-critical pairs flagged with safety_critical=true and timing_warning_level set
- TIMING_EVALUATION.md is a complete timing rule inventory referencing both product and pair data
- TIMING_REVIEW.md surfaces all safety-critical and business decisions for Chris review
- All content attribution uses "expert consensus" or "clinical practice data" — never "podcast"
</success_criteria>

<output>
After completion, create `.planning/phases/07-timing-rules/07-02-SUMMARY.md`
</output>
