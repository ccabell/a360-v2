---
phase: 01-citations
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - Fable Docs/DOSSIER_COMPILE_PIPELINE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "DOSSIER_COMPILE_PIPELINE.md STEP 5 instructs future compiles to always capture pmid, doi, url, page_number on evidence_links inserts"
    - "All three citation source types (PubMed, FDA, YouTube) have populated url values in evidence_links (verified via SQL)"
  artifacts:
    - path: "Fable Docs/DOSSIER_COMPILE_PIPELINE.md"
      provides: "Updated compile pipeline with citation locator capture instructions"
      contains: "page_number"
  key_links:
    - from: "Fable Docs/DOSSIER_COMPILE_PIPELINE.md"
      to: "evidence_links inserts in STEP 5"
      via: "Instruction paragraph in STEP 5"
      pattern: "pmid.*doi.*url.*page_number"
---

<objective>
Update the compile pipeline doc to capture citation locators on all future evidence_links inserts, then verify the citation data is correctly populated across all three source types.

Purpose: Ensure future dossier compiles produce complete evidence_links rows (no data gaps going forward) and confirm the backfill scripts from Plans 01 and 02 populated the data correctly.

Output: Updated DOSSIER_COMPILE_PIPELINE.md, confirmed correct citation data in evidence_links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-citations/01-CONTEXT.md
@.planning/phases/01-citations/01-RESEARCH.md

@Fable Docs/DOSSIER_COMPILE_PIPELINE.md
@.planning/phases/01-citations/01-01-SUMMARY.md
@.planning/phases/01-citations/01-02-SUMMARY.md

<interfaces>
<!-- DOSSIER_COMPILE_PIPELINE.md structure -->
The file has STEP 1 through STEP 5 in the master prompt section.
STEP 4 = COMPILE THREE LENSES (lens priority + posture).
STEP 5 = EMIT (write to real tables).

STEP 5 already mentions:
```
INSERT evidence_links rows for every asserted claim (offering_id, field_name, source, pmid/doi/
chunk_id, snippet, authority_rank, similarity). A clinical section with zero backing
evidence_links is INVALID.
```

The gap: STEP 5 does not explicitly instruct to always populate `url` (constructed from pmid/doi),
or `page_number` (for FDA sources). These fields exist but were never mentioned in the compile prompt.

Per-type locator fields that must be captured (from CONTEXT.md specifics):
| Source Type | Required Fields |
|-------------|----------------|
| pubmed | pmid, doi, url = https://pubmed.ncbi.nlm.nih.gov/{pmid}/ |
| youtube | url = https://www.youtube.com/watch?v={video_id}&t={start_time_int}s |
| fda_label | url = https://www.accessdata.fda.gov/..., page_number |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DOSSIER_COMPILE_PIPELINE.md with citation locator capture instructions</name>
  <files>Fable Docs/DOSSIER_COMPILE_PIPELINE.md</files>
  <read_first>
    - Fable Docs/DOSSIER_COMPILE_PIPELINE.md (full file — find STEP 5 EMIT section, understand current evidence_links INSERT instruction)
    - .planning/phases/01-citations/01-CONTEXT.md (decision D-12: add one paragraph to STEP 5)
  </read_first>
  <action>
Per D-12: Add a `CITATION LOCATOR CAPTURE` paragraph to the STEP 5 - EMIT section of DOSSIER_COMPILE_PIPELINE.md, immediately after the existing `INSERT evidence_links rows` instruction.

Insert this exact text after the evidence_links INSERT line in STEP 5:

```
  CITATION LOCATOR CAPTURE — for every evidence_links INSERT, always populate these URL/locator
  fields so the citation UI can render clickable links without post-hoc backfill:
    - source='pubmed': set pmid (from CrossRef or source metadata), doi, and
      url = 'https://pubmed.ncbi.nlm.nih.gov/{pmid}/' (constructed from pmid).
    - source='fda_label': set url = the public FDA Access Data URL
      (https://www.accessdata.fda.gov/drugsatfda_docs/label/{year}/{NDA}s{supplement}lbl.pdf)
      and page_number = the page in the PDF where the cited content appears.
    - source='youtube': set url = 'https://www.youtube.com/watch?v={video_id}&t={start_seconds}s'
      where start_seconds comes from the matched CMS transcript chunk's start_time field.
    - source='manufacturer'/'ifu': set url = the public document URL if available.
  An evidence_links row with source='pubmed' but pmid=NULL or url='' is INCOMPLETE — resolve
  before marking the dossier compile as done.
```

This is a doc-only change. Do NOT modify any code files.
  </action>
  <verify>
    <automated>grep -c "CITATION LOCATOR CAPTURE" "Fable Docs/DOSSIER_COMPILE_PIPELINE.md"</automated>
  </verify>
  <acceptance_criteria>
    - `Fable Docs/DOSSIER_COMPILE_PIPELINE.md` contains the string "CITATION LOCATOR CAPTURE"
    - `Fable Docs/DOSSIER_COMPILE_PIPELINE.md` contains "pubmed.ncbi.nlm.nih.gov/{pmid}/"
    - `Fable Docs/DOSSIER_COMPILE_PIPELINE.md` contains "page_number"
    - `Fable Docs/DOSSIER_COMPILE_PIPELINE.md` contains "accessdata.fda.gov"
    - `Fable Docs/DOSSIER_COMPILE_PIPELINE.md` contains "start_seconds"
    - The paragraph appears within STEP 5 (after the evidence_links INSERT instruction, before STEP 6 or HARD RULES)
  </acceptance_criteria>
  <done>DOSSIER_COMPILE_PIPELINE.md STEP 5 now instructs future compiles to always capture pmid, doi, url, and page_number on evidence_links inserts. No data gap can recur for new dossiers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Demo gate — verify citation data in evidence_links</name>
  <files>n/a</files>
  <action>
This is a human verification checkpoint. Present the user with the verification steps below.

All three citation data gaps have been fixed by Plans 01 and 02:
- PubMed: pmid + url populated via CrossRef backfill
- FDA: url populated with public accessdata.fda.gov links
- YouTube: url updated with timestamp deep-links
- Pipeline doc updated to prevent future gaps

Run these SQL verification queries via Supabase MCP on project aejskvmpembryunnbgrk before presenting to user:
1. `SELECT source, COUNT(*), COUNT(NULLIF(url, '')) as has_url FROM evidence_links GROUP BY source`
2. `SELECT source, url, pmid FROM evidence_links WHERE offering_id = '4b92be36-e84e-432c-8549-f5d85a767fdb' AND url != '' LIMIT 10`

Present results to user for review.

**Scope note:** This phase (01-citations) addressed evidence_links DATA gaps only. The Research/Evidence tab UI is currently wired to mock data (`lib/mock/research-data.ts`). Wiring the UI to read from real evidence_links data is out of scope for this phase and will be addressed in a follow-on phase.
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>(a) DB assertions pass: PubMed rows have pmid + pubmed.ncbi.nlm.nih.gov URLs, YouTube rows have timestamp deep-links (&t= in url), FDA rows have accessdata.fda.gov URLs. (b) UI wiring (Research tab reading from evidence_links vs mock data) is explicitly out of scope for this phase and will be addressed in a follow-on phase.</done>
</task>

</tasks>

<verification>
1. Pipeline doc: `grep "CITATION LOCATOR CAPTURE" "Fable Docs/DOSSIER_COMPILE_PIPELINE.md"` returns match
2. PubMed complete: `SELECT COUNT(*) FROM evidence_links WHERE source='pubmed' AND url LIKE 'https://pubmed.ncbi.nlm.nih.gov/%'` >= 3
3. FDA complete: `SELECT COUNT(*) FROM evidence_links WHERE source='fda_label' AND url LIKE 'https://www.accessdata.fda.gov/%'` > 0
4. YouTube complete: `SELECT COUNT(*) FROM evidence_links WHERE source='youtube' AND url LIKE '%&t=%'` > 0
5. No regressions: `SELECT COUNT(*) FROM evidence_links WHERE source='pubmed' AND pmid IS NOT NULL` >= 3
</verification>

<success_criteria>
- DOSSIER_COMPILE_PIPELINE.md has citation locator capture instructions in STEP 5
- All three citation source types have clickable URLs in evidence_links (DB-level)
- Chris has verified the data looks correct (or approved the gap reports)
- UI wiring to real evidence_links data is acknowledged as a follow-on phase
</success_criteria>

<output>
After completion, create `.planning/phases/01-citations/01-03-SUMMARY.md`
</output>
